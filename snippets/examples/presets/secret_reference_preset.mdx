{/* Generated from examples/presets/secret_reference_preset.yaml by scripts/sync_examples.py. Do not edit. */}

```yaml examples/presets/secret_reference_preset.yaml
# A preset that bakes a secret REFERENCE, not a credential: the `!ENV ${VAR}`
# value is kept verbatim in the store and resolved from the environment at bind.
base_tool: echo_tool
description: A configured variant that carries a service token as a reference.
fixed_kwargs:
  api_token: "!ENV ${SERVICE_API_TOKEN}"
```
