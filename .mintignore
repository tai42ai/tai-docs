# Local tool state that lives beside the site but is not documentation. The
# hosted build never sees these; `mint validate` and `mint broken-links` run in
# a working checkout that does, and would otherwise parse them as MDX.
.venv
.ruff_cache
.pytest_cache
