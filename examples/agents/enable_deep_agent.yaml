#| validate: manifest
# Enable just the LangGraph deep agent: its module registers the agent on import.
# The agents router that exposes the SSE run stream is part of the default router
# set, so it is already mounted.
agents:
  - title: langchain-deep-agent
    module: tai42_agents.langchain_deep_agent
