## What

<!-- What docs change does this PR make? -->

## Why

<!-- Why is the change needed? Link related issues. -->

## Checklist

- [ ] The changed pages render correctly in the local preview (`mint dev`)
- [ ] Links and code samples are valid
- [ ] If `scripts/` changed: lint and tests pass (`uv run --no-sync ruff check .`, `uv run --no-sync pytest`)
