version: 2
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
  # The Python dependencies resolve from path sources on the sibling tai42 checkout,
  # which Dependabot cannot retrieve; the docs' Python pins follow the platform's releases.
