---
title: "Tools (tai42_contract.tools)"
description: "Tool registration types and the app tools namespace."
icon: "wrench"
---


Tools contract: the ``ToolInfo`` model + the ``AppTools`` tool/toolkit
registration sub-protocol. Vendor return types (fastmcp ``Tool``, langchain
``StructuredTool``) are ``TYPE_CHECKING``-only.

``AppTools`` is the ``app.tools`` namespace of the assembled facade
(`tai42_contract.app`).

## AppTools

`tai42_contract.tools.AppTools`

```python
class AppTools(Protocol)
```

Tool + toolkit registration / lookup surface.

### Members

#### tool_title

`tai42_contract.tools.AppTools.tool_title`

```python
tool_title(self, func: Callable[..., object]) -> str
```

**Parameters**

| Parameter | Type | Default | Description |
|---|---|---|---|
| `func` | `Callable[..., object]` | — | — |


#### get_tool

`tai42_contract.tools.AppTools.get_tool`

```python
async get_tool(self, key: str) -> Tool
```

**Parameters**

| Parameter | Type | Default | Description |
|---|---|---|---|
| `key` | `str` | — | — |


#### get_tools

`tai42_contract.tools.AppTools.get_tools`

```python
async get_tools(self) -> dict[str, Tool]
```


#### get_client_tools

`tai42_contract.tools.AppTools.get_client_tools`

```python
async get_client_tools(self, names: list[str] | None = None) -> list[StructuredTool]
```

**Parameters**

| Parameter | Type | Default | Description |
|---|---|---|---|
| `names` | `list[str] \| None` | `None` | — |


#### run_tool

`tai42_contract.tools.AppTools.run_tool`

```python
async run_tool(self, key: str, arguments: dict[str, Any], *, offload_sync: bool = False) -> Any
```

**Parameters**

| Parameter | Type | Default | Description |
|---|---|---|---|
| `key` | `str` | — | — |
| `arguments` | `dict[str, Any]` | — | — |
| `offload_sync` | `bool` | `False` | — |


#### remove_tool

`tai42_contract.tools.AppTools.remove_tool`

```python
remove_tool(self, name: str) -> None
```

**Parameters**

| Parameter | Type | Default | Description |
|---|---|---|---|
| `name` | `str` | — | — |


#### register_tool_info

`tai42_contract.tools.AppTools.register_tool_info`

```python
register_tool_info(self, name: str, combos: Sequence[Sequence[ExtensionElement]] | None = None) -> None
```

**Parameters**

| Parameter | Type | Default | Description |
|---|---|---|---|
| `name` | `str` | — | — |
| `combos` | `Sequence[Sequence[ExtensionElement]] \| None` | `None` | — |


#### unregister_tool_info

`tai42_contract.tools.AppTools.unregister_tool_info`

```python
unregister_tool_info(self, name: str) -> None
```

**Parameters**

| Parameter | Type | Default | Description |
|---|---|---|---|
| `name` | `str` | — | — |


#### unregister_tool_base

`tai42_contract.tools.AppTools.unregister_tool_base`

```python
unregister_tool_base(self, tool_name: str) -> list[str]
```

**Parameters**

| Parameter | Type | Default | Description |
|---|---|---|---|
| `tool_name` | `str` | — | — |


#### tool_refs_extractor

`tai42_contract.tools.AppTools.tool_refs_extractor`

```python
tool_refs_extractor(self, name: str) -> ToolRefsExtractor | None
```

**Parameters**

| Parameter | Type | Default | Description |
|---|---|---|---|
| `name` | `str` | — | — |


#### register_rename_referee

`tai42_contract.tools.AppTools.register_rename_referee`

```python
register_rename_referee(self, provider: ToolRenameReferee) -> None
```

Register a `ToolRenameReferee` consulted before a tool rename.

A plugin holding tool-name references calls this through the ``tai42_app``
handle when its module loads. Every registered referee is asked for the
old name on a rename; any non-empty answer blocks the rename and its
descriptions name the holders. Registering the same provider object twice
raises loudly — a double registration is a plugin bug, never a silent
duplicate consult.

**Parameters**

| Parameter | Type | Default | Description |
|---|---|---|---|
| `provider` | `ToolRenameReferee` | — | — |



## ToolInfo

`tai42_contract.tools.ToolInfo`

```python
class ToolInfo(BaseModel)
```

Descriptor for a registered tool.

``name`` is the registered key; ``base`` is the underlying tool it was
bound from (a branch tool names itself, e.g. ``name_chain``).

**Attributes**

| Attribute | Type |
|---|---|
| `name` | `str` |
| `base` | `str` |
| `title` | `str` |
| `description` | `str` |


## ToolInvocation

`tai42_contract.tools.invocation.ToolInvocation`

```python
class ToolInvocation(BaseModel)
```

The tool execution currently in flight: ``tool_name`` is the invoked
tool's registered name. Frozen — a deposited invocation is a fact of the
active execution, never mutated in place.

**Attributes**

| Attribute | Type |
|---|---|
| `model_config` | — |
| `tool_name` | `str` |


## ToolRefsExtractor

`tai42_contract.tools.ToolRefsExtractor`

```python
ToolRefsExtractor = Callable[[dict[str, Any]], Iterable[str]]
```


## ToolRenameReferee

`tai42_contract.tools.ToolRenameReferee`

```python
ToolRenameReferee = Callable[[str], Awaitable[list[str]]]
```


## current_tool_invocation

`tai42_contract.tools.invocation.current_tool_invocation`

```python
current_tool_invocation() -> ToolInvocation | None
```

The tool execution in flight for the current context, or ``None`` when no
tool is executing.


## reset_current_tool_invocation

`tai42_contract.tools.invocation.reset_current_tool_invocation`

```python
reset_current_tool_invocation(token: Token[ToolInvocation | None]) -> None
```

Restore the in-flight tool to the value captured in ``token`` by the
matching `set_current_tool_invocation` call.

**Parameters**

| Parameter | Type | Default | Description |
|---|---|---|---|
| `token` | `Token[ToolInvocation \| None]` | — | — |


## set_current_tool_invocation

`tai42_contract.tools.invocation.set_current_tool_invocation`

```python
set_current_tool_invocation(invocation: ToolInvocation) -> Token[ToolInvocation | None]
```

Deposit ``invocation`` as the in-flight tool for the current context; pass
the returned token to `reset_current_tool_invocation` to restore the
previous value. Nested deposits (a tool invoking another) re-set for the inner
call and restore the outer value on reset — ContextVar token discipline.

**Parameters**

| Parameter | Type | Default | Description |
|---|---|---|---|
| `invocation` | `ToolInvocation` | — | — |

