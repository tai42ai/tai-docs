---
title: "tai notifications"
description: "Read and send internal notifications."
icon: "terminal"
---

Read and send internal notifications.

```console
$ tai notifications [OPTIONS] COMMAND [ARGS]...
```

## `tai notifications list`

List the internal notifications, newest-first.

Example: `tai notifications list`

```console
$ tai notifications list [OPTIONS]
```

| Option | Description |
| --- | --- |
| `--json` / `--no-json` | Emit raw JSON instead of human tables. |

## `tai notifications notify`

Send a human a one-way, fire-and-forget notification.

`--media` and `--template` are JSON strings (the nested shapes have no flat-flag
form); each is validated into its contract model before the request, so a malformed
value raises loudly here rather than at the server.

Example: `tai notifications notify "Deploy finished" --channel telegram`

```console
$ tai notifications notify [OPTIONS] MESSAGE
```

| Argument | Description |
| --- | --- |
| `MESSAGE` | The notification text shown to the human. _(required)_ |

| Option | Description |
| --- | --- |
| `--channel` `TEXT` | Named channel to send on; omit to record to the internal sink. |
| `--recipient` `TEXT` | Optional per-call address (chat id, phone number, ...). |
| `--media` `TEXT` | JSON array of display-media items sent WITH the message, e.g. '[&#123;"kind":"image","url":"https://…"&#125;]'. |
| `--template` `TEXT` | JSON object for an out-of-window template send, e.g. '&#123;"name":"order_update","language":"en_US"&#125;'. |
| `--json` / `--no-json` | Emit raw JSON instead of human tables. |
