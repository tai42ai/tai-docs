---
title: "tai version"
description: "Show the tai-skeleton version and the versions of its key dependencies."
icon: "terminal"
---

Show the tai-skeleton version and the versions of its key dependencies.

```console
$ tai version [OPTIONS]
```

| Option | Description |
| --- | --- |
| `--json` / `--no-json` | Emit raw JSON instead of human tables. |
