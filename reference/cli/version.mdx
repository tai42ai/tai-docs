---
title: "tai version"
description: "Show the installed tai42 packages and the versions of key CLI dependencies."
icon: "terminal"
---

Show the installed tai42 packages and the versions of key CLI dependencies.

```console
$ tai version [OPTIONS]
```

| Option | Description |
| --- | --- |
| `--json` / `--no-json` | Emit raw JSON instead of human tables. |
