---
title: "Utilities"
description: "Shared helpers and re-exported types the SDK exposes."
icon: "wrench"
---


Shared helpers and re-exported types the SDK exposes.

## AdapterCompileResult

```ts
type AdapterCompileResult = { jq: string; ok: true } | { error: string; ok: false }
```

A compile outcome: the jq program, or a human error to surface loudly.


## AdapterMapping

```ts
function AdapterMapping(props: AdapterMappingProps): ReactNode
```

**Props**

| Prop | Type | Description |
|---|---|---|
| `declaredInput` | `readonly string[]` | The declared input keys of the update jq — one row each. Empty ⇒ raw jq only. |
| `onChange` | `(adapterJq: string \| null) => void` | Emits the adapter to store: a non-empty jq, or `null` for no adapter (never `''`). |
| `sources?` | `BindingSourceSchemas` | The output / input field paths a picker offers. |
| `suggestions?` | `readonly TemplateJqSuggestion[]` | Template jq offered as `tjq_<name>({…})` inserts in the jq escape hatch. |
| `value` | `string` | The current adapter jq (compiled or authored) stored on the update; `''` ⇒ none stored. |

**Related:** [AdapterMappingProps](/reference/studio-sdk/utilities#adaptermappingprops)


## AdapterMappingProps

```ts
interface AdapterMappingProps
```

**Properties**

| Property | Type | Description |
|---|---|---|
| `declaredInput` | `readonly string[]` | The declared input keys of the update jq — one row each. Empty ⇒ raw jq only. |
| `onChange` | `(adapterJq: string \| null) => void` | Emits the adapter to store: a non-empty jq, or `null` for no adapter (never `''`). |
| `sources?` | `BindingSourceSchemas` | The output / input field paths a picker offers. |
| `suggestions?` | `readonly TemplateJqSuggestion[]` | Template jq offered as `tjq_<name>({…})` inserts in the jq escape hatch. |
| `value` | `string` | The current adapter jq (compiled or authored) stored on the update; `''` ⇒ none stored. |

**Related:** [BindingSourceSchemas](/reference/studio-sdk/utilities#bindingsourceschemas), [TemplateJqSuggestion](/reference/studio-sdk/utilities#templatejqsuggestion)


## ApiClient

```ts
type ApiClient = ReturnType<typeof createApiClient>
```


## BindingJqField

```ts
function BindingJqField(props: BindingJqFieldProps): ReactNode
```

**Props**

| Prop | Type | Description |
|---|---|---|
| `description?` | `ReactNode` | — |
| `error?` | `string` | — |
| `hideLabel?` | `boolean` | Render the field's own visible label visually hidden while keeping it as the control's accessible name — for a field mounted under a group header that already shows the label (a `TemplatedTextField` slot). Off by default, so a bare use (a mapping row's jq) keeps its visible label. |
| `label` | `string` | — |
| `onChange` | `(value: string) => void` | — |
| `placeholder?` | `string` | — |
| `suggestions?` | `readonly TemplateJqSuggestion[]` | Template jq offered as `tjq_<name>({…})` inserts under the field. |
| `value` | `string` | — |

**Related:** [BindingJqFieldProps](/reference/studio-sdk/utilities#bindingjqfieldprops)


## BindingJqFieldProps

```ts
interface BindingJqFieldProps
```

**Properties**

| Property | Type | Description |
|---|---|---|
| `description?` | `ReactNode` | — |
| `error?` | `string` | — |
| `hideLabel?` | `boolean` | Render the field's own visible label visually hidden while keeping it as the control's accessible name — for a field mounted under a group header that already shows the label (a `TemplatedTextField` slot). Off by default, so a bare use (a mapping row's jq) keeps its visible label. |
| `label` | `string` | — |
| `onChange` | `(value: string) => void` | — |
| `placeholder?` | `string` | — |
| `suggestions?` | `readonly TemplateJqSuggestion[]` | Template jq offered as `tjq_<name>({…})` inserts under the field. |
| `value` | `string` | — |

**Related:** [TemplateJqSuggestion](/reference/studio-sdk/utilities#templatejqsuggestion)


## BindingSourceSchemas

```ts
interface BindingSourceSchemas
```

The schemas a field picker draws from when authoring an injection or an adapter:
the run's `output` and `input` fields (the state's fields ride each
`BindingStateOption`). Absent roots offer no picked field — the author uses
a literal or a jq expression instead.

**Properties**

| Property | Type | Description |
|---|---|---|
| `error?` | `string` | The tool-schema fetch failed — the field control shows a note but stays usable. |
| `input?` | `readonly SchemaFieldPath[]` | — |
| `loading?` | `boolean` | The tool-schema fetch that feeds the pickers is in flight. |
| `output?` | `readonly SchemaFieldPath[]` | — |

**Related:** [SchemaFieldPath](/reference/studio-sdk/utilities#schemafieldpath)


## BindingStateOption

```ts
interface BindingStateOption
```

One state the editor may bind, with the templates already attached to it and its fields.

**Properties**

| Property | Type | Description |
|---|---|---|
| `attachedTemplates?` | `readonly string[]` | Templates already attached to the state (the rest attach on save). |
| `fields?` | `readonly SchemaFieldPath[]` | The state's own record fields, for the `state` source of a field picker. |
| `name` | `string` | — |

**Related:** [SchemaFieldPath](/reference/studio-sdk/utilities#schemafieldpath)


## BindingTemplateJqOption

```ts
interface BindingTemplateJqOption
```

One template jq of an attached template, as the pickers present it.

**Properties**

| Property | Type | Description |
|---|---|---|
| `description?` | `string` | — |
| `name` | `string` | — |
| `params?` | `readonly string[]` | An `input` jq's declared params / an `update` jq's declared input keys. |
| `purpose` | `"input" \| "update"` | — |
| `writes?` | `readonly readonly string[][]` | An `update` jq's declared write paths, each a list of key segments. |


## BindingTemplateOption

```ts
interface BindingTemplateOption
```

One template the editor may attach to a state (attach-on-use), with its template jq.

**Properties**

| Property | Type | Description |
|---|---|---|
| `attached?` | `boolean` | Whether this template is already attached to the chosen state (else: attach on save). |
| `description?` | `string` | — |
| `name` | `string` | — |
| `templateJq` | `readonly BindingTemplateJqOption[]` | — |

**Related:** [BindingTemplateJqOption](/reference/studio-sdk/utilities#bindingtemplatejqoption)


## BindingTemplatedJqField

```ts
function BindingTemplatedJqField(props: BindingTemplatedJqFieldProps): ReactNode
```

**Props**

| Prop | Type | Description |
|---|---|---|
| `description?` | `string` | — |
| `disabled?` | `boolean` | — |
| `error?` | `string` | — |
| `label` | `string` | — |
| `onChange` | `(value: { content?: string; id?: string; kwargs?: Record<string, unknown> } \| null) => void` | — |
| `placeholder?` | `string` | — |
| `required?` | `boolean` | The subject/adapter slots require a source; the rest are optional (`null` clears). |
| `suggestions?` | `readonly TemplateJqSuggestion[]` | — |
| `templates?` | `TemplatedTextCatalog` | — |
| `value` | `{ content?: string; id?: string; kwargs?: Record<string, unknown> } \| null` | — |

**Related:** [BindingTemplatedJqFieldProps](/reference/studio-sdk/utilities#bindingtemplatedjqfieldprops)


## BindingTemplatedJqFieldProps

```ts
interface BindingTemplatedJqFieldProps
```

**Properties**

| Property | Type | Description |
|---|---|---|
| `description?` | `string` | — |
| `disabled?` | `boolean` | — |
| `error?` | `string` | — |
| `label` | `string` | — |
| `onChange` | `(value: { content?: string; id?: string; kwargs?: Record<string, unknown> } \| null) => void` | — |
| `placeholder?` | `string` | — |
| `required?` | `boolean` | The subject/adapter slots require a source; the rest are optional (`null` clears). |
| `suggestions?` | `readonly TemplateJqSuggestion[]` | — |
| `templates?` | `TemplatedTextCatalog` | — |
| `value` | `{ content?: string; id?: string; kwargs?: Record<string, unknown> } \| null` | — |

**Related:** [TemplateJqSuggestion](/reference/studio-sdk/utilities#templatejqsuggestion), [TemplatedTextCatalog](/reference/studio-sdk/utilities#templatedtextcatalog)


## FeatureDisabled

```ts
function FeatureDisabled(props: FeatureDisabledProps): ReactNode
```

The muted, honest OFF state for a feature whose store this deployment has not
configured. A `role="status"` EmptyState (never the loud alert ErrorState): the
feature is not broken, it is simply off, and the copy shows the server's own
remediation message.

**Props**

| Prop | Type | Description |
|---|---|---|
| `feature` | `string` | The human name of the OFF feature, e.g. `Interactions`. |
| `message` | `string` | The server's own remediation line, shown verbatim (never a client-composed string). |

**Related:** [FeatureDisabledProps](/reference/studio-sdk/utilities#featuredisabledprops)


## FeatureDisabledProps

```ts
interface FeatureDisabledProps
```

**Properties**

| Property | Type | Description |
|---|---|---|
| `feature` | `string` | The human name of the OFF feature, e.g. `Interactions`. |
| `message` | `string` | The server's own remediation line, shown verbatim (never a client-composed string). |


## FieldRoot

```ts
type FieldRoot = "output" | "input"
```

Which root of `{ output, input }` a picked field reads.


## FieldSource

```ts
interface FieldSource
```

A picked field: a path into `output` or `input`.

**Properties**

| Property | Type | Description |
|---|---|---|
| `kind` | `"field"` | — |
| `path` | `readonly string[]` | — |
| `root` | `FieldRoot` | — |

**Related:** [FieldRoot](/reference/studio-sdk/utilities#fieldroot)


## InheritedSubject

```ts
interface InheritedSubject
```

The subject/scope of the target's own (inherited) binding for a state this one also names.

**Properties**

| Property | Type | Description |
|---|---|---|
| `scope_expr` | `{ content?: string; id?: string; kwargs?: Record<string, unknown> } \| null` | — |
| `subject_expr` | `{ content?: string; id?: string; kwargs?: Record<string, unknown> }` | — |


## InjectionList

```ts
function InjectionList(props: InjectionListProps): ReactNode
```

**Props**

| Prop | Type | Description |
|---|---|---|
| `injections` | `readonly ({ into: string; jq: { content?: string; id?: string; kwargs?: Record<string, unknown> } \| null; template_jq: string \| null })[]` | — |
| `inputJq` | `readonly ResolvedTemplateJq[]` | — |
| `onChange` | `(injections: readonly ({ into: string; jq: { content?: string; id?: string; kwargs?: Record<string, unknown> } \| null; template_jq: string \| null })[]) => void` | — |
| `suggestions?` | `readonly TemplateJqSuggestion[]` | — |
| `templates?` | `TemplatedTextCatalog` | — |

**Related:** [InjectionListProps](/reference/studio-sdk/utilities#injectionlistprops)


## InjectionListProps

```ts
interface InjectionListProps
```

**Properties**

| Property | Type | Description |
|---|---|---|
| `injections` | `readonly ({ into: string; jq: { content?: string; id?: string; kwargs?: Record<string, unknown> } \| null; template_jq: string \| null })[]` | — |
| `inputJq` | `readonly ResolvedTemplateJq[]` | — |
| `onChange` | `(injections: readonly ({ into: string; jq: { content?: string; id?: string; kwargs?: Record<string, unknown> } \| null; template_jq: string \| null })[]) => void` | — |
| `suggestions?` | `readonly TemplateJqSuggestion[]` | — |
| `templates?` | `TemplatedTextCatalog` | — |

**Related:** [ResolvedTemplateJq](/reference/studio-sdk/utilities#resolvedtemplatejq), [TemplateJqSuggestion](/reference/studio-sdk/utilities#templatejqsuggestion), [TemplatedTextCatalog](/reference/studio-sdk/utilities#templatedtextcatalog)


## JqSource

```ts
interface JqSource
```

A raw jq expression over `{ output, input }`.

**Properties**

| Property | Type | Description |
|---|---|---|
| `expr` | `string` | — |
| `kind` | `"jq"` | — |


## LiteralSource

```ts
interface LiteralSource
```

A hardcoded literal, authored as JSON text (`"a"`, `42`, `true`, `{…}`).

**Properties**

| Property | Type | Description |
|---|---|---|
| `json` | `string` | — |
| `kind` | `"literal"` | — |


## MappingRow

```ts
interface MappingRow
```

One mapping row: a target key of the update jq's declared input + its value source.

**Properties**

| Property | Type | Description |
|---|---|---|
| `source` | `MappingSource` | — |
| `target` | `string` | — |

**Related:** [MappingSource](/reference/studio-sdk/utilities#mappingsource)


## MappingSource

```ts
type MappingSource = FieldSource | LiteralSource | JqSource
```

The three value sources one mapping row may carry — exactly one at a time.

**Related:** [FieldSource](/reference/studio-sdk/utilities#fieldsource), [JqSource](/reference/studio-sdk/utilities#jqsource), [LiteralSource](/reference/studio-sdk/utilities#literalsource)


## ResolvedTemplateJq

```ts
interface ResolvedTemplateJq extends BindingTemplateJqOption
```

One template jq of an attached template, resolved to the reference a binding stores.

**Properties**

| Property | Type | Description |
|---|---|---|
| `description?` | `string` | — |
| `name` | `string` | — |
| `params?` | `readonly string[]` | An `input` jq's declared params / an `update` jq's declared input keys. |
| `purpose` | `"input" \| "update"` | — |
| `ref` | `string` | The reference a binding writes: `name`, or `template.name` when ambiguous. |
| `template` | `string` | The owning template. |
| `writes?` | `readonly readonly string[][]` | An `update` jq's declared write paths, each a list of key segments. |

**Related:** [BindingTemplateJqOption](/reference/studio-sdk/utilities#bindingtemplatejqoption)


## SchemaEditor

```ts
function SchemaEditor(props: SchemaEditorProps): ReactNode
```

**Props**

| Prop | Type | Description |
|---|---|---|
| `description?` | `string` | — |
| `disabled?` | `boolean` | — |
| `idPrefix?` | `string` | — |
| `label?` | `string` | — |
| `onChange` | `(change: SchemaEditorChange) => void` | Fired on every edit with the parsed schema (or `null`) and its validity. |
| `requireTitle` | `boolean` | When true, a top-level `"title"` is required (the `response_format` contract). |
| `value` | `Record<string, unknown> \| null` | The schema dict to seed the editor with, or `null` for an empty editor. |

**Related:** [SchemaEditorProps](/reference/studio-sdk/utilities#schemaeditorprops)


## SchemaEditorChange

```ts
interface SchemaEditorChange
```

What `SchemaEditor` reports up on every edit.

**Properties**

| Property | Type | Description |
|---|---|---|
| `schema` | `Record<string, unknown> \| null` | The authored schema dict when valid and non-empty; `null` when the editor is empty. |
| `valid` | `boolean` | False when the text is non-empty but unparseable or failing lint — gate submit on this. |


## SchemaEditorProps

```ts
interface SchemaEditorProps
```

**Properties**

| Property | Type | Description |
|---|---|---|
| `description?` | `string` | — |
| `disabled?` | `boolean` | — |
| `idPrefix?` | `string` | — |
| `label?` | `string` | — |
| `onChange` | `(change: SchemaEditorChange) => void` | Fired on every edit with the parsed schema (or `null`) and its validity. |
| `requireTitle` | `boolean` | When true, a top-level `"title"` is required (the `response_format` contract). |
| `value` | `Record<string, unknown> \| null` | The schema dict to seed the editor with, or `null` for an empty editor. |

**Related:** [SchemaEditorChange](/reference/studio-sdk/utilities#schemaeditorchange)


## SchemaFieldPath

```ts
interface SchemaFieldPath
```

One selectable field of a schema: the record path + a human label.

**Properties**

| Property | Type | Description |
|---|---|---|
| `label` | `string` | — |
| `path` | `readonly string[]` | — |


## SchemaLintResult

```ts
interface SchemaLintResult
```

The parse/lint outcome for one editor text buffer.

**Properties**

| Property | Type | Description |
|---|---|---|
| `error` | `string \| undefined` | A loud, human-readable message when valid is false; else `undefined`. |
| `schema` | `Record<string, unknown> \| null` | The parsed schema dict when the text is a syntactically valid JSON object, or `null` when the text is empty or does not parse. Carries the parsed object even when a lint (e.g. a missing `title`) fails, so the preview can still show the authored shape — the consumer gates on valid, never on this alone. |
| `valid` | `boolean` | True when the text is empty or a valid schema. False when the text is non-empty but fails to parse or fails a structural lint — the consumer blocks submit. |


## StateAttachRow

```ts
function StateAttachRow(props: StateAttachRowProps): ReactNode
```

**Props**

| Prop | Type | Description |
|---|---|---|
| `attach` | `{ input_injections: ({ into: string; jq: { content?: string; id?: string; kwargs?: Record<string, unknown> } \| null; template_jq: string \| null })[]; scope_expr: { content?: string; id?: string; kwargs?: Record<string, unknown> } \| null; state: string; subject_expr: { content?: string; id?: string; kwargs?: Record<string, unknown> }; templates: string[]; updates: ({ adapter: { content?: string; id?: string; kwargs?: Record<string, unknown> } \| null; jq: { content?: string; id?: string; kwargs?: Record<string, unknown> } \| null; op_id: { content?: string; id?: string; kwargs?: Record<string, unknown> } \| null; template_jq: string \| null })[] }` | — |
| `inherited?` | `InheritedSubject` | The target's own binding subject for this state, when it also binds it (precedence). |
| `onChange` | `(attach: { input_injections: ({ into: string; jq: { content?: string; id?: string; kwargs?: Record<string, unknown> } \| null; template_jq: string \| null })[]; scope_expr: { content?: string; id?: string; kwargs?: Record<string, unknown> } \| null; state: string; subject_expr: { content?: string; id?: string; kwargs?: Record<string, unknown> }; templates: string[]; updates: ({ adapter: { content?: string; id?: string; kwargs?: Record<string, unknown> } \| null; jq: { content?: string; id?: string; kwargs?: Record<string, unknown> } \| null; op_id: { content?: string; id?: string; kwargs?: Record<string, unknown> } \| null; template_jq: string \| null })[] }) => void` | — |
| `onRemove` | `() => void` | — |
| `sources?` | `BindingSourceSchemas` | — |
| `statesCatalog` | `readonly BindingStateOption[]` | — |
| `subjectError?` | `string` | — |
| `templates?` | `TemplatedTextCatalog` | The stored templates a templated-text field's id picker offers. |
| `templatesCatalog` | `readonly BindingTemplateOption[]` | — |

**Related:** [StateAttachRowProps](/reference/studio-sdk/utilities#stateattachrowprops)


## StateAttachRowProps

```ts
interface StateAttachRowProps
```

**Properties**

| Property | Type | Description |
|---|---|---|
| `attach` | `{ input_injections: ({ into: string; jq: { content?: string; id?: string; kwargs?: Record<string, unknown> } \| null; template_jq: string \| null })[]; scope_expr: { content?: string; id?: string; kwargs?: Record<string, unknown> } \| null; state: string; subject_expr: { content?: string; id?: string; kwargs?: Record<string, unknown> }; templates: string[]; updates: ({ adapter: { content?: string; id?: string; kwargs?: Record<string, unknown> } \| null; jq: { content?: string; id?: string; kwargs?: Record<string, unknown> } \| null; op_id: { content?: string; id?: string; kwargs?: Record<string, unknown> } \| null; template_jq: string \| null })[] }` | — |
| `inherited?` | `InheritedSubject` | The target's own binding subject for this state, when it also binds it (precedence). |
| `onChange` | `(attach: { input_injections: ({ into: string; jq: { content?: string; id?: string; kwargs?: Record<string, unknown> } \| null; template_jq: string \| null })[]; scope_expr: { content?: string; id?: string; kwargs?: Record<string, unknown> } \| null; state: string; subject_expr: { content?: string; id?: string; kwargs?: Record<string, unknown> }; templates: string[]; updates: ({ adapter: { content?: string; id?: string; kwargs?: Record<string, unknown> } \| null; jq: { content?: string; id?: string; kwargs?: Record<string, unknown> } \| null; op_id: { content?: string; id?: string; kwargs?: Record<string, unknown> } \| null; template_jq: string \| null })[] }) => void` | — |
| `onRemove` | `() => void` | — |
| `sources?` | `BindingSourceSchemas` | — |
| `statesCatalog` | `readonly BindingStateOption[]` | — |
| `subjectError?` | `string` | — |
| `templates?` | `TemplatedTextCatalog` | The stored templates a templated-text field's id picker offers. |
| `templatesCatalog` | `readonly BindingTemplateOption[]` | — |

**Related:** [BindingSourceSchemas](/reference/studio-sdk/utilities#bindingsourceschemas), [BindingStateOption](/reference/studio-sdk/utilities#bindingstateoption), [BindingTemplateOption](/reference/studio-sdk/utilities#bindingtemplateoption), [InheritedSubject](/reference/studio-sdk/utilities#inheritedsubject), [TemplatedTextCatalog](/reference/studio-sdk/utilities#templatedtextcatalog)


## StateBindingEditor

```ts
function StateBindingEditor(props: StateBindingEditorProps): ReactNode
```

**Props**

| Prop | Type | Description |
|---|---|---|
| `error?` | `string` | The catalog fetch failed. |
| `inherited?` | `{ states: ({ input_injections: ({ into: string; jq: { content?: string; id?: string; kwargs?: Record<string, unknown> } \| null; template_jq: string \| null })[]; scope_expr: { content?: string; id?: string; kwargs?: Record<string, unknown> } \| null; state: string; subject_expr: { content?: string; id?: string; kwargs?: Record<string, unknown> }; templates: string[]; updates: ({ adapter: { content?: string; id?: string; kwargs?: Record<string, unknown> } \| null; jq: { content?: string; id?: string; kwargs?: Record<string, unknown> } \| null; op_id: { content?: string; id?: string; kwargs?: Record<string, unknown> } \| null; template_jq: string \| null })[] })[] } \| null` | The TARGET's own binding (e.g. the preset a route dispatches), passed by the door screen — never fetched here. A state this binding names that the inherited one also names shows a precedence warning on its card. |
| `loading?` | `boolean` | The catalog fetch is in flight. |
| `onChange` | `(value: { states: ({ input_injections: ({ into: string; jq: { content?: string; id?: string; kwargs?: Record<..., ...> } \| null; template_jq: string \| null })[]; scope_expr: { content?: string; id?: string; kwargs?: Record<string, unknown> } \| null; state: string; subject_expr: { content?: string; id?: string; kwargs?: Record<string, unknown> }; templates: string[]; updates: ({ adapter: { content?: string; id?: string; kwargs?: Record<..., ...> } \| null; jq: { content?: string; id?: string; kwargs?: Record<..., ...> } \| null; op_id: { content?: string; id?: string; kwargs?: Record<..., ...> } \| null; template_jq: string \| null })[] })[] } \| null) => void` | — |
| `sources?` | `BindingSourceSchemas` | The run's output/input field paths a picker offers when a schema is known. |
| `statesCatalog` | `readonly BindingStateOption[]` | The states this door may bind, each with its attached templates and fields. |
| `templatedTextTemplates?` | `TemplatedTextCatalog` | The stored templates a templated-text field's id picker offers, resolved by the door. |
| `templatesCatalog` | `readonly BindingTemplateOption[]` | The templates the editor may attach (attach-on-use), each with its template jq. |
| `value` | `{ states: ({ input_injections: ({ into: string; jq: { content?: string; id?: string; kwargs?: Record<string, unknown> } \| null; template_jq: string \| null })[]; scope_expr: { content?: string; id?: string; kwargs?: Record<string, unknown> } \| null; state: string; subject_expr: { content?: string; id?: string; kwargs?: Record<string, unknown> }; templates: string[]; updates: ({ adapter: { content?: string; id?: string; kwargs?: Record<string, unknown> } \| null; jq: { content?: string; id?: string; kwargs?: Record<string, unknown> } \| null; op_id: { content?: string; id?: string; kwargs?: Record<string, unknown> } \| null; template_jq: string \| null })[] })[] } \| null` | The binding, or `null` when the door binds no state. |

**Related:** [StateBindingEditorProps](/reference/studio-sdk/utilities#statebindingeditorprops)


## StateBindingEditorProps

```ts
interface StateBindingEditorProps
```

**Properties**

| Property | Type | Description |
|---|---|---|
| `error?` | `string` | The catalog fetch failed. |
| `inherited?` | `{ states: ({ input_injections: ({ into: string; jq: { content?: string; id?: string; kwargs?: Record<string, unknown> } \| null; template_jq: string \| null })[]; scope_expr: { content?: string; id?: string; kwargs?: Record<string, unknown> } \| null; state: string; subject_expr: { content?: string; id?: string; kwargs?: Record<string, unknown> }; templates: string[]; updates: ({ adapter: { content?: string; id?: string; kwargs?: Record<string, unknown> } \| null; jq: { content?: string; id?: string; kwargs?: Record<string, unknown> } \| null; op_id: { content?: string; id?: string; kwargs?: Record<string, unknown> } \| null; template_jq: string \| null })[] })[] } \| null` | The TARGET's own binding (e.g. the preset a route dispatches), passed by the door screen — never fetched here. A state this binding names that the inherited one also names shows a precedence warning on its card. |
| `loading?` | `boolean` | The catalog fetch is in flight. |
| `onChange` | `(value: { states: ({ input_injections: ({ into: string; jq: { content?: string; id?: string; kwargs?: Record<..., ...> } \| null; template_jq: string \| null })[]; scope_expr: { content?: string; id?: string; kwargs?: Record<string, unknown> } \| null; state: string; subject_expr: { content?: string; id?: string; kwargs?: Record<string, unknown> }; templates: string[]; updates: ({ adapter: { content?: string; id?: string; kwargs?: Record<..., ...> } \| null; jq: { content?: string; id?: string; kwargs?: Record<..., ...> } \| null; op_id: { content?: string; id?: string; kwargs?: Record<..., ...> } \| null; template_jq: string \| null })[] })[] } \| null) => void` | — |
| `sources?` | `BindingSourceSchemas` | The run's output/input field paths a picker offers when a schema is known. |
| `statesCatalog` | `readonly BindingStateOption[]` | The states this door may bind, each with its attached templates and fields. |
| `templatedTextTemplates?` | `TemplatedTextCatalog` | The stored templates a templated-text field's id picker offers, resolved by the door. |
| `templatesCatalog` | `readonly BindingTemplateOption[]` | The templates the editor may attach (attach-on-use), each with its template jq. |
| `value` | `{ states: ({ input_injections: ({ into: string; jq: { content?: string; id?: string; kwargs?: Record<string, unknown> } \| null; template_jq: string \| null })[]; scope_expr: { content?: string; id?: string; kwargs?: Record<string, unknown> } \| null; state: string; subject_expr: { content?: string; id?: string; kwargs?: Record<string, unknown> }; templates: string[]; updates: ({ adapter: { content?: string; id?: string; kwargs?: Record<string, unknown> } \| null; jq: { content?: string; id?: string; kwargs?: Record<string, unknown> } \| null; op_id: { content?: string; id?: string; kwargs?: Record<string, unknown> } \| null; template_jq: string \| null })[] })[] } \| null` | The binding, or `null` when the door binds no state. |

**Related:** [BindingSourceSchemas](/reference/studio-sdk/utilities#bindingsourceschemas), [BindingStateOption](/reference/studio-sdk/utilities#bindingstateoption), [BindingTemplateOption](/reference/studio-sdk/utilities#bindingtemplateoption), [TemplatedTextCatalog](/reference/studio-sdk/utilities#templatedtextcatalog)


## StateBindingSection

```ts
function StateBindingSection(props: StateBindingSectionProps): ReactNode
```

**Props**

| Prop | Type | Description |
|---|---|---|
| `error?` | `string` | The catalog fetch failed. |
| `inherited?` | `{ states: ({ input_injections: ({ into: string; jq: { content?: string; id?: string; kwargs?: Record<string, unknown> } \| null; template_jq: string \| null })[]; scope_expr: { content?: string; id?: string; kwargs?: Record<string, unknown> } \| null; state: string; subject_expr: { content?: string; id?: string; kwargs?: Record<string, unknown> }; templates: string[]; updates: ({ adapter: { content?: string; id?: string; kwargs?: Record<string, unknown> } \| null; jq: { content?: string; id?: string; kwargs?: Record<string, unknown> } \| null; op_id: { content?: string; id?: string; kwargs?: Record<string, unknown> } \| null; template_jq: string \| null })[] })[] } \| null` | The TARGET's own binding (e.g. the preset a route dispatches), passed by the door screen — never fetched here. A state this binding names that the inherited one also names shows a precedence warning on its card. |
| `label?` | `string` | Copy on the disclosure toggle. |
| `loading?` | `boolean` | The catalog fetch is in flight. |
| `onChange` | `(value: { states: ({ input_injections: ({ into: string; jq: { content?: string; id?: string; kwargs?: Record<..., ...> } \| null; template_jq: string \| null })[]; scope_expr: { content?: string; id?: string; kwargs?: Record<string, unknown> } \| null; state: string; subject_expr: { content?: string; id?: string; kwargs?: Record<string, unknown> }; templates: string[]; updates: ({ adapter: { content?: string; id?: string; kwargs?: Record<..., ...> } \| null; jq: { content?: string; id?: string; kwargs?: Record<..., ...> } \| null; op_id: { content?: string; id?: string; kwargs?: Record<..., ...> } \| null; template_jq: string \| null })[] })[] } \| null) => void` | — |
| `sources?` | `BindingSourceSchemas` | The run's output/input field paths a picker offers when a schema is known. |
| `statesCatalog` | `readonly BindingStateOption[]` | The states this door may bind, each with its attached templates and fields. |
| `templatedTextTemplates?` | `TemplatedTextCatalog` | The stored templates a templated-text field's id picker offers, resolved by the door. |
| `templatesCatalog` | `readonly BindingTemplateOption[]` | The templates the editor may attach (attach-on-use), each with its template jq. |
| `value` | `{ states: ({ input_injections: ({ into: string; jq: { content?: string; id?: string; kwargs?: Record<string, unknown> } \| null; template_jq: string \| null })[]; scope_expr: { content?: string; id?: string; kwargs?: Record<string, unknown> } \| null; state: string; subject_expr: { content?: string; id?: string; kwargs?: Record<string, unknown> }; templates: string[]; updates: ({ adapter: { content?: string; id?: string; kwargs?: Record<string, unknown> } \| null; jq: { content?: string; id?: string; kwargs?: Record<string, unknown> } \| null; op_id: { content?: string; id?: string; kwargs?: Record<string, unknown> } \| null; template_jq: string \| null })[] })[] } \| null` | The binding, or `null` when the door binds no state. |

**Related:** [StateBindingSectionProps](/reference/studio-sdk/utilities#statebindingsectionprops)


## StateBindingSectionProps

```ts
interface StateBindingSectionProps extends StateBindingEditorProps
```

**Properties**

| Property | Type | Description |
|---|---|---|
| `error?` | `string` | The catalog fetch failed. |
| `inherited?` | `{ states: ({ input_injections: ({ into: string; jq: { content?: string; id?: string; kwargs?: Record<string, unknown> } \| null; template_jq: string \| null })[]; scope_expr: { content?: string; id?: string; kwargs?: Record<string, unknown> } \| null; state: string; subject_expr: { content?: string; id?: string; kwargs?: Record<string, unknown> }; templates: string[]; updates: ({ adapter: { content?: string; id?: string; kwargs?: Record<string, unknown> } \| null; jq: { content?: string; id?: string; kwargs?: Record<string, unknown> } \| null; op_id: { content?: string; id?: string; kwargs?: Record<string, unknown> } \| null; template_jq: string \| null })[] })[] } \| null` | The TARGET's own binding (e.g. the preset a route dispatches), passed by the door screen — never fetched here. A state this binding names that the inherited one also names shows a precedence warning on its card. |
| `label?` | `string` | Copy on the disclosure toggle. |
| `loading?` | `boolean` | The catalog fetch is in flight. |
| `onChange` | `(value: { states: ({ input_injections: ({ into: string; jq: { content?: string; id?: string; kwargs?: Record<..., ...> } \| null; template_jq: string \| null })[]; scope_expr: { content?: string; id?: string; kwargs?: Record<string, unknown> } \| null; state: string; subject_expr: { content?: string; id?: string; kwargs?: Record<string, unknown> }; templates: string[]; updates: ({ adapter: { content?: string; id?: string; kwargs?: Record<..., ...> } \| null; jq: { content?: string; id?: string; kwargs?: Record<..., ...> } \| null; op_id: { content?: string; id?: string; kwargs?: Record<..., ...> } \| null; template_jq: string \| null })[] })[] } \| null) => void` | — |
| `sources?` | `BindingSourceSchemas` | The run's output/input field paths a picker offers when a schema is known. |
| `statesCatalog` | `readonly BindingStateOption[]` | The states this door may bind, each with its attached templates and fields. |
| `templatedTextTemplates?` | `TemplatedTextCatalog` | The stored templates a templated-text field's id picker offers, resolved by the door. |
| `templatesCatalog` | `readonly BindingTemplateOption[]` | The templates the editor may attach (attach-on-use), each with its template jq. |
| `value` | `{ states: ({ input_injections: ({ into: string; jq: { content?: string; id?: string; kwargs?: Record<string, unknown> } \| null; template_jq: string \| null })[]; scope_expr: { content?: string; id?: string; kwargs?: Record<string, unknown> } \| null; state: string; subject_expr: { content?: string; id?: string; kwargs?: Record<string, unknown> }; templates: string[]; updates: ({ adapter: { content?: string; id?: string; kwargs?: Record<string, unknown> } \| null; jq: { content?: string; id?: string; kwargs?: Record<string, unknown> } \| null; op_id: { content?: string; id?: string; kwargs?: Record<string, unknown> } \| null; template_jq: string \| null })[] })[] } \| null` | The binding, or `null` when the door binds no state. |

**Related:** [BindingSourceSchemas](/reference/studio-sdk/utilities#bindingsourceschemas), [BindingStateOption](/reference/studio-sdk/utilities#bindingstateoption), [BindingTemplateOption](/reference/studio-sdk/utilities#bindingtemplateoption), [StateBindingEditorProps](/reference/studio-sdk/utilities#statebindingeditorprops), [TemplatedTextCatalog](/reference/studio-sdk/utilities#templatedtextcatalog)


## SubjectScopeFields

```ts
function SubjectScopeFields(props: SubjectScopeFieldsProps): ReactNode
```

**Props**

| Prop | Type | Description |
|---|---|---|
| `onScopeChange` | `(value: { content?: string; id?: string; kwargs?: Record<string, unknown> } \| null) => void` | — |
| `onSubjectChange` | `(value: { content?: string; id?: string; kwargs?: Record<string, unknown> }) => void` | — |
| `scopeExpr` | `{ content?: string; id?: string; kwargs?: Record<string, unknown> } \| null` | — |
| `subjectError?` | `string` | — |
| `subjectExpr` | `{ content?: string; id?: string; kwargs?: Record<string, unknown> }` | — |
| `suggestions?` | `readonly TemplateJqSuggestion[]` | — |
| `templates?` | `TemplatedTextCatalog` | — |

**Related:** [SubjectScopeFieldsProps](/reference/studio-sdk/utilities#subjectscopefieldsprops)


## SubjectScopeFieldsProps

```ts
interface SubjectScopeFieldsProps
```

**Properties**

| Property | Type | Description |
|---|---|---|
| `onScopeChange` | `(value: { content?: string; id?: string; kwargs?: Record<string, unknown> } \| null) => void` | — |
| `onSubjectChange` | `(value: { content?: string; id?: string; kwargs?: Record<string, unknown> }) => void` | — |
| `scopeExpr` | `{ content?: string; id?: string; kwargs?: Record<string, unknown> } \| null` | — |
| `subjectError?` | `string` | — |
| `subjectExpr` | `{ content?: string; id?: string; kwargs?: Record<string, unknown> }` | — |
| `suggestions?` | `readonly TemplateJqSuggestion[]` | — |
| `templates?` | `TemplatedTextCatalog` | — |

**Related:** [TemplateJqSuggestion](/reference/studio-sdk/utilities#templatejqsuggestion), [TemplatedTextCatalog](/reference/studio-sdk/utilities#templatedtextcatalog)


## TemplateJqSuggestion

```ts
interface TemplateJqSuggestion
```

One template jq offered as an insertable autocomplete entry.

**Properties**

| Property | Type | Description |
|---|---|---|
| `description?` | `string` | — |
| `purpose` | `"input" \| "update"` | — |
| `ref` | `string` | The jq reference, e.g. `active` or the attachment-qualified `tally.bump`. |


## TemplatedText

```ts
type TemplatedText = z.infer<typeof templatedText>
```


## TemplatedTextCatalog

```ts
interface TemplatedTextCatalog
```

The stored templates a `TemplatedText` field's id picker offers, with the
fetch's loading/error state — resolved by the door screen and passed to the
editor, which holds no data edge of its own. Absent leaves every templated-text
field with the inline source only.

**Properties**

| Property | Type | Description |
|---|---|---|
| `error?` | `string` | — |
| `loading?` | `boolean` | — |
| `onRetry?` | `() => void` | — |
| `templates?` | `readonly TemplatedTextTemplateOption[]` | — |

**Related:** [TemplatedTextTemplateOption](/reference/studio-sdk/components#templatedtexttemplateoption)


## UpdateList

```ts
function UpdateList(props: UpdateListProps): ReactNode
```

**Props**

| Prop | Type | Description |
|---|---|---|
| `onChange` | `(updates: readonly ({ adapter: { content?: string; id?: string; kwargs?: Record<string, unknown> } \| null; jq: { content?: string; id?: string; kwargs?: Record<string, unknown> } \| null; op_id: { content?: string; id?: string; kwargs?: Record<string, unknown> } \| null; template_jq: string \| null })[]) => void` | — |
| `sources?` | `BindingSourceSchemas` | — |
| `suggestions?` | `readonly TemplateJqSuggestion[]` | — |
| `templates?` | `TemplatedTextCatalog` | — |
| `updateJq` | `readonly ResolvedTemplateJq[]` | — |
| `updates` | `readonly ({ adapter: { content?: string; id?: string; kwargs?: Record<string, unknown> } \| null; jq: { content?: string; id?: string; kwargs?: Record<string, unknown> } \| null; op_id: { content?: string; id?: string; kwargs?: Record<string, unknown> } \| null; template_jq: string \| null })[]` | — |

**Related:** [UpdateListProps](/reference/studio-sdk/utilities#updatelistprops)


## UpdateListProps

```ts
interface UpdateListProps
```

**Properties**

| Property | Type | Description |
|---|---|---|
| `onChange` | `(updates: readonly ({ adapter: { content?: string; id?: string; kwargs?: Record<string, unknown> } \| null; jq: { content?: string; id?: string; kwargs?: Record<string, unknown> } \| null; op_id: { content?: string; id?: string; kwargs?: Record<string, unknown> } \| null; template_jq: string \| null })[]) => void` | — |
| `sources?` | `BindingSourceSchemas` | — |
| `suggestions?` | `readonly TemplateJqSuggestion[]` | — |
| `templates?` | `TemplatedTextCatalog` | — |
| `updateJq` | `readonly ResolvedTemplateJq[]` | — |
| `updates` | `readonly ({ adapter: { content?: string; id?: string; kwargs?: Record<string, unknown> } \| null; jq: { content?: string; id?: string; kwargs?: Record<string, unknown> } \| null; op_id: { content?: string; id?: string; kwargs?: Record<string, unknown> } \| null; template_jq: string \| null })[]` | — |

**Related:** [BindingSourceSchemas](/reference/studio-sdk/utilities#bindingsourceschemas), [ResolvedTemplateJq](/reference/studio-sdk/utilities#resolvedtemplatejq), [TemplateJqSuggestion](/reference/studio-sdk/utilities#templatejqsuggestion), [TemplatedTextCatalog](/reference/studio-sdk/utilities#templatedtextcatalog)


## appendTjq

```ts
function appendTjq(current: string, ref: string): string
```

Append a `tjq_<name>({…})` template call to the current expression (space-separated when non-empty).

**Parameters**

| Parameter | Type | Default | Description |
|---|---|---|---|
| `current` | `string` | — | — |
| `ref` | `string` | — | — |


## comboElementNames

```ts
function comboElementNames(
  combo: readonly (string | { config: z.ZodRecord<z.ZodString, z.ZodUnknown>; name: string })[],
): string[]
```

Project a combo's elements to their bare extension names (drops any `config`).

**Parameters**

| Parameter | Type | Default | Description |
|---|---|---|---|
| `combo` | `readonly (string \| { config: z.ZodRecord<z.ZodString, z.ZodUnknown>; name: string })[]` | — | — |


## compileAdapter

```ts
function compileAdapter(rows: readonly MappingRow[]): AdapterCompileResult
```

Compile a mapping form into ONE adapter jq that constructs the update jq's
declared input over `{ output, input }`. An empty form compiles to the
empty object `{}`; a blank target, a duplicate target, or an invalid source is a
loud error.

**Parameters**

| Parameter | Type | Default | Description |
|---|---|---|---|
| `rows` | `readonly MappingRow[]` | — | — |

**Related:** [AdapterCompileResult](/reference/studio-sdk/utilities#adaptercompileresult), [MappingRow](/reference/studio-sdk/utilities#mappingrow)


## defaultRowsForInput

```ts
function defaultRowsForInput(params: readonly string[]): MappingRow[]
```

One row per declared input key, each defaulting to a picked `output` field.

**Parameters**

| Parameter | Type | Default | Description |
|---|---|---|---|
| `params` | `readonly string[]` | — | — |

**Related:** [MappingRow](/reference/studio-sdk/utilities#mappingrow)


## downloadBlob

```ts
function downloadBlob(blob: Blob, filename: string): void
```

Stream a `Blob` (an export- or content-route response) to the user as a file
download via a transient object URL. The object URL is revoked on the next tick —
revoking it synchronously can cancel the download before the browser commits it.

**Parameters**

| Parameter | Type | Default | Description |
|---|---|---|---|
| `blob` | `Blob` | — | — |
| `filename` | `string` | — | — |


## effectiveHidden

```ts
function effectiveHidden(
  overlayHidden: boolean | null | undefined,
  declaredHidden: boolean,
): boolean
```

Effective hidden after the tri-state override. `overlayHidden` is the overlay's
`hidden` (`true`/`false`), or `null`/`undefined` when the overlay defers or has
no row; `declaredHidden` is the plugin's own `hidden`. Overlay opinion wins;
absence defers to the declaration.

**Parameters**

| Parameter | Type | Default | Description |
|---|---|---|---|
| `overlayHidden` | `boolean \| null \| undefined` | — | — |
| `declaredHidden` | `boolean` | — | — |


## encodeTemplateSegment

```ts
function encodeTemplateSegment(template: string): string
```

Encode a template-name slug for a jq identifier: each `-` becomes `_`. Under the slug
invariant (no consecutive or trailing hyphens) the result carries no `__` and no
trailing `_`, which is what makes the first `__` in a call name the template boundary.

**Parameters**

| Parameter | Type | Default | Description |
|---|---|---|---|
| `template` | `string` | — | — |


## errorMessage

```ts
function errorMessage(error: unknown): string
```

Normalize an unknown thrown/rejected value (a TanStack Query `error`, a
rejected mutation, a zod/`ApiError`) into a human-readable string for the loud
`ErrorState` / `Field` surfaces. This only turns a caught value into
displayable text — it never swallows the failure; the caller decides where to
show it, and the message is always shown, never blank.

**Parameters**

| Parameter | Type | Default | Description |
|---|---|---|---|
| `error` | `unknown` | — | — |


## extensionElementName

```ts
function extensionElementName(
  element: string | { config: z.ZodRecord<z.ZodString, z.ZodUnknown>; name: string },
): string
```

The bare extension name of one combo element (drops any `config`).

**Parameters**

| Parameter | Type | Default | Description |
|---|---|---|---|
| `element` | `string \| { config: z.ZodRecord<z.ZodString, z.ZodUnknown>; name: string }` | — | — |


## extensionsQueryKey

```ts
const extensionsQueryKey: readonly ["extensions"]
```

The extension catalog (`GET /api/extensions`). The extensions feature keys its
read-only catalog on it; a tool's extension-combo save (in the tools feature)
creates or tears down branch tools, so it invalidates this key too — a
cross-feature reference that must resolve to one authoritative constant.


## featureDisabledMessage

```ts
function featureDisabledMessage(error: unknown): string
```

The remediation line for a refusal: the skeleton's own `message` (which names the
missing configuration), else its machine `code`. The client NEVER composes this —
a refusal carrying neither is a contract breach and raises.

**Parameters**

| Parameter | Type | Default | Description |
|---|---|---|---|
| `error` | `unknown` | — | — |


## fieldPathToJq

```ts
function fieldPathToJq(root: FieldRoot, path: readonly string[]): string
```

A picked field rendered as a jq path, e.g. `.output.user.name` / `.input["odd key"]`.

**Parameters**

| Parameter | Type | Default | Description |
|---|---|---|---|
| `root` | `FieldRoot` | — | — |
| `path` | `readonly string[]` | — | — |

**Related:** [FieldRoot](/reference/studio-sdk/utilities#fieldroot)


## fieldPathsFromSchema

```ts
function fieldPathsFromSchema(schema: unknown): SchemaFieldPath[]
```

Flatten a JSON schema's `properties` into selectable field paths (top-level and
every nested object level), each labelled with its dotted path — the picker source
for the adapter's `output`/`input` roots. A non-object or property-less schema
yields no fields (the field control then falls back to a free path input).

**Parameters**

| Parameter | Type | Default | Description |
|---|---|---|---|
| `schema` | `unknown` | — | — |

**Related:** [SchemaFieldPath](/reference/studio-sdk/utilities#schemafieldpath)


## findByRef

```ts
function findByRef(
  ref: string,
  resolved: readonly ResolvedTemplateJq[],
): ResolvedTemplateJq | undefined
```

Find one resolved template jq by the reference a binding stores.

**Parameters**

| Parameter | Type | Default | Description |
|---|---|---|---|
| `ref` | `string` | — | — |
| `resolved` | `readonly ResolvedTemplateJq[]` | — | — |

**Related:** [ResolvedTemplateJq](/reference/studio-sdk/utilities#resolvedtemplatejq)


## generateTemplateCall

```ts
function generateTemplateCall(ref: string, rows: readonly MappingRow[]): string
```

The single-expression form of a named template update built from a mapping form:
`tjq_<enc(template)>__<name>({…})` for a qualified ref, `tjq_<name>({…})` for a bare
one (see the module note on the `__` boundary invariant).

**Parameters**

| Parameter | Type | Default | Description |
|---|---|---|---|
| `ref` | `string` | — | — |
| `rows` | `readonly MappingRow[]` | — | — |

**Related:** [MappingRow](/reference/studio-sdk/utilities#mappingrow)


## hiddenToolNames

```ts
function hiddenToolNames(
  tagEntries: readonly ({ badges: string[]; hidden: boolean; name: string; tags: string[] })[],
  overlayRows: readonly ({ badges: string[]; display_name: string | null; folder_id: string | null; hidden: boolean | null; tags: string[]; tool_name: string })[],
): Set<string>
```

The set of tool names whose EFFECTIVE visibility is hidden, from the native
declarations (`listToolTags`) merged with the tool_meta overlay (`listToolMeta`).
A picker excludes exactly these names from its options; the server stays the
authority (a slipped-through name is rejected loudly there), so a name in the set
that is not among a picker's options is simply inert.

The union of both sources is walked so an overlay row that force-hides a tool
carrying no native tag entry is still caught, and an overlay `false` on a
plugin-hidden tool correctly keeps it OUT of the set (unhidden).

**Parameters**

| Parameter | Type | Default | Description |
|---|---|---|---|
| `tagEntries` | `readonly ({ badges: string[]; hidden: boolean; name: string; tags: string[] })[]` | — | — |
| `overlayRows` | `readonly ({ badges: string[]; display_name: string \| null; folder_id: string \| null; hidden: boolean \| null; tags: string[]; tool_name: string })[]` | — | — |


## isFeatureDisabled

```ts
function isFeatureDisabled(error: unknown): boolean
```

True when `error` is a disabled-feature refusal from the skeleton: an HTTP 501, or
a `code` ending `-not-configured`. The `code` is authoritative; the 501 status is
the fallback signal — either alone is enough.

**Parameters**

| Parameter | Type | Default | Description |
|---|---|---|---|
| `error` | `unknown` | — | — |


## jqKey

```ts
function jqKey(key: string): string
```

An object key rendered for jq: a bare identifier, or a quoted string otherwise.

**Parameters**

| Parameter | Type | Default | Description |
|---|---|---|---|
| `key` | `string` | — | — |


## lintSchemaText

```ts
function lintSchemaText(text: string, requireTitle: boolean): SchemaLintResult
```

Parse and structurally lint a JSON-Schema editor buffer.

 - Empty text is VALID (an unset, optional schema) with a `null` schema.
 - Malformed JSON is INVALID; the parser message is surfaced verbatim.
 - A top-level value that is not a JSON object (an array or a scalar) is INVALID.
 - When `requireTitle`, a top-level string `"title"` is REQUIRED (the
   `response_format` contract the backend enforces); its absence is INVALID.

The parsed object is returned untouched — no key is normalized, reordered, or
dropped — so a schema carrying `$defs`/`anyOf` round-trips exactly.

**Parameters**

| Parameter | Type | Default | Description |
|---|---|---|---|
| `text` | `string` | — | — |
| `requireTitle` | `boolean` | — | — |

**Related:** [SchemaLintResult](/reference/studio-sdk/utilities#schemalintresult)


## mergeToolBadges

```ts
function mergeToolBadges(
  nativeBadges: readonly string[],
  overlayBadges: readonly string[],
): readonly string[]
```

A tool's effective badges: native ∪ overlay, deduped and case-insensitively sorted.
The atomic rule every badge surface merges by, so the tools list and the pickers
show one and the same set.

**Parameters**

| Parameter | Type | Default | Description |
|---|---|---|---|
| `nativeBadges` | `readonly string[]` | — | — |
| `overlayBadges` | `readonly string[]` | — | — |


## parseAdapter

```ts
function parseAdapter(jq: string): MappingRow[] | null
```

Recover the mapping rows from a stored adapter jq, or `null` if it is not the generated shape.

**Parameters**

| Parameter | Type | Default | Description |
|---|---|---|---|
| `jq` | `string` | — | — |

**Related:** [MappingRow](/reference/studio-sdk/utilities#mappingrow)


## parseFieldPath

```ts
function parseFieldPath(value: string): FieldSource | null
```

Recover a field source from a pure path expression, or `null` if it is not one.

**Parameters**

| Parameter | Type | Default | Description |
|---|---|---|---|
| `value` | `string` | — | — |

**Related:** [FieldSource](/reference/studio-sdk/utilities#fieldsource)


## parseTemplateCall

```ts
function parseTemplateCall(expr: string): { adapter: string; callName: string } | null
```

Recover `{ callName, adapter }` from a `tjq_<callName>(<adapter>)` call, or `null`
otherwise. `callName` is the RAW jq identifier — NEVER split here (a jq name may carry
its own `__` past the template boundary); resolve it to a catalog ref with
`resolveCallName`.

**Parameters**

| Parameter | Type | Default | Description |
|---|---|---|---|
| `expr` | `string` | — | — |


## resolveCallName

```ts
function resolveCallName(
  callName: string,
  catalog: readonly BindingTemplateOption[],
): string | null
```

Resolve a jq call name (`enc(template)__name`, or a bare `name`) back to the catalog
ref, driven by the catalog — never by string splitting, since a jq name may carry its
own `__` past the template boundary and `enc` maps `-`→`_`. Under the template-slug
invariant (no consecutive/trailing hyphens ⇒ `enc(template)` has no `__` and no
trailing `_`) the FIRST `__` is the one true boundary, so the longest encoded-template
prefix whose remainder is a jq the template declares is the unique qualified match;
failing that, a bare name a single template declares. `null` when nothing claims it.

**Parameters**

| Parameter | Type | Default | Description |
|---|---|---|---|
| `callName` | `string` | — | — |
| `catalog` | `readonly BindingTemplateOption[]` | — | — |

**Related:** [BindingTemplateOption](/reference/studio-sdk/utilities#bindingtemplateoption)


## resolveTemplateJq

```ts
function resolveTemplateJq(
  templateNames: readonly string[],
  catalog: readonly BindingTemplateOption[],
  purpose?: "input" | "update",
): ResolvedTemplateJq[]
```

Every template jq the given attached templates offer for a purpose (or all
purposes when `purpose` is omitted), each carrying the reference a binding stores.

**Parameters**

| Parameter | Type | Default | Description |
|---|---|---|---|
| `templateNames` | `readonly string[]` | — | — |
| `catalog` | `readonly BindingTemplateOption[]` | — | — |
| `purpose` *(optional)* | `"input" \| "update"` | — | — |

**Related:** [BindingTemplateOption](/reference/studio-sdk/utilities#bindingtemplateoption), [ResolvedTemplateJq](/reference/studio-sdk/utilities#resolvedtemplatejq)


## rowValueJq

```ts
function rowValueJq(source: MappingSource): AdapterCompileResult
```

The jq fragment ONE row's value compiles to — the row's "show jq".

**Parameters**

| Parameter | Type | Default | Description |
|---|---|---|---|
| `source` | `MappingSource` | — | — |

**Related:** [AdapterCompileResult](/reference/studio-sdk/utilities#adaptercompileresult), [MappingSource](/reference/studio-sdk/utilities#mappingsource)


## stateTemplatesKey

```ts
const stateTemplatesKey: readonly ["states", "state-templates"]
```

The state-template document catalog (`GET /api/state-templates`). Read by the states
feature AND every state-binding door screen (the templates a binding attaches),
keyed here so the two never fetch the same catalog under diverging tuples.


## statesCatalogFromList

```ts
function statesCatalogFromList(
  states: readonly ({ default_subject_kind: string; description: string; effective_schema?: Record<string, unknown> | null; name: string; regimes?: Record<string, unknown>[] | null; retention_days: number | null; schema: z.ZodRecord<z.ZodString, z.ZodUnknown>; subject_kinds: string[]; updated_at: string | null })[],
): BindingStateOption[]
```

The states catalog from `listStates()`; per-state attachments are resolved lazily elsewhere.

**Parameters**

| Parameter | Type | Default | Description |
|---|---|---|---|
| `states` | `readonly ({ default_subject_kind: string; description: string; effective_schema?: Record<string, unknown> \| null; name: string; regimes?: Record<string, unknown>[] \| null; retention_days: number \| null; schema: z.ZodRecord<z.ZodString, z.ZodUnknown>; subject_kinds: string[]; updated_at: string \| null })[]` | — | — |

**Related:** [BindingStateOption](/reference/studio-sdk/utilities#bindingstateoption)


## statesListKey

```ts
const statesListKey: readonly ["states", "list"]
```

The state catalog (`GET /api/states`). The states feature owns it, and every door
screen that hosts the state-binding editor (a preset, a route, a hook, a schedule)
reads the same catalog to populate the binding's state picker — so both key the one
authoritative tuple and a single fetch serves both.


## subMcpKey

```ts
const subMcpKey: readonly ["sub-mcp"]
```

The discovered sub-MCP mounts (`GET /api/sub-mcp`). The settings feature reads
this map (its scope mapper lists the mounts) while the manifest feature owns the
create/delete controls that mutate it; both must key and invalidate the exact
same tuple, so it lives here rather than as a literal duplicated across the two
features (which cannot import each other).


## templatesCatalogFromList

```ts
function templatesCatalogFromList(
  items: readonly ({ attached_to: number; declarations: Record<string, unknown> | null; description: string; kind: "state-template"; name: string; parameters: z.ZodDefault<z.ZodRecord<z.ZodString, z.ZodUnknown>>; reconcile: { close: string; resolutions: string; view: string } | null; regimes: Record<string, unknown>[]; schema: z.ZodRecord<z.ZodString, z.ZodUnknown>; shipped_default: boolean; template_jq: Record<string, { description: string; jq: string; params: string[]; purpose: "input" | "update"; reads: string[][]; writes: string[][] }> | null; trace: z.ZodDefault<z.ZodRecord<z.ZodString, z.ZodUnknown>> })[],
): BindingTemplateOption[]
```

The template catalog (with each template's template jq) from `listStateTemplates()`.

**Parameters**

| Parameter | Type | Default | Description |
|---|---|---|---|
| `items` | `readonly ({ attached_to: number; declarations: Record<string, unknown> \| null; description: string; kind: "state-template"; name: string; parameters: z.ZodDefault<z.ZodRecord<z.ZodString, z.ZodUnknown>>; reconcile: { close: string; resolutions: string; view: string } \| null; regimes: Record<string, unknown>[]; schema: z.ZodRecord<z.ZodString, z.ZodUnknown>; shipped_default: boolean; template_jq: Record<string, { description: string; jq: string; params: string[]; purpose: "input" \| "update"; reads: string[][]; writes: string[][] }> \| null; trace: z.ZodDefault<z.ZodRecord<z.ZodString, z.ZodUnknown>> })[]` | — | — |

**Related:** [BindingTemplateOption](/reference/studio-sdk/utilities#bindingtemplateoption)


## tokensPayloadKey

```ts
const tokensPayloadKey: readonly ["auth-tokens-payload"]
```

The api-key payloads; read by settings AND the hooks execution-key picker.


## toolBadgesByName

```ts
function toolBadgesByName(
  tagEntries: readonly ({ badges: string[]; hidden: boolean; name: string; tags: string[] })[],
  overlayRows: readonly ({ badges: string[]; display_name: string | null; folder_id: string | null; hidden: boolean | null; tags: string[]; tool_name: string })[],
): Readonly<Record<string, readonly string[]>>
```

The raw-name → merged-badges map a tool picker reads to render the SELECTED tool's
badges, from the native declarations (`listToolTags`) merged with the tool_meta
overlay (`listToolMeta`) — the SAME union the tools list shows. Only tools with at
least one badge appear; a name with none is simply absent (the picker shows no
chips). Both sources' names are walked, so an overlay row that badges a tool
carrying no native tag entry is still surfaced.

**Parameters**

| Parameter | Type | Default | Description |
|---|---|---|---|
| `tagEntries` | `readonly ({ badges: string[]; hidden: boolean; name: string; tags: string[] })[]` | — | — |
| `overlayRows` | `readonly ({ badges: string[]; display_name: string \| null; folder_id: string \| null; hidden: boolean \| null; tags: string[]; tool_name: string })[]` | — | — |


## toolsListKey

```ts
const toolsListKey: readonly ["tools"]
```

The registered-tool master list (`GET /api/tools`).

