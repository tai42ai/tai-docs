---
title: "Components"
description: "The design-system components — inputs, layout, tables, pickers, and disclosure primitives."
icon: "shapes"
---


The design-system components — inputs, layout, tables, pickers, and disclosure primitives.

## AgentsIcon

```ts
const AgentsIcon: IconComponent
```

Agents: a bot — antenna, panel head with eyes and mouth, side ports.

**Related:** [IconComponent](/reference/studio-sdk/components#iconcomponent)


## AlertTriangleIcon

```ts
const AlertTriangleIcon: IconComponent
```

Warning.

**Related:** [IconComponent](/reference/studio-sdk/components#iconcomponent)


## ArrowDownIcon

```ts
const ArrowDownIcon: IconComponent
```

Move the item one place later in an ordered list.

**Related:** [IconComponent](/reference/studio-sdk/components#iconcomponent)


## ArrowLeftIcon

```ts
const ArrowLeftIcon: IconComponent
```

Back / previous.

**Related:** [IconComponent](/reference/studio-sdk/components#iconcomponent)


## ArrowUpIcon

```ts
const ArrowUpIcon: IconComponent
```

Move the item one place earlier in an ordered list.

**Related:** [IconComponent](/reference/studio-sdk/components#iconcomponent)


## BADGES_NOTE

```ts
const BADGES_NOTE: "Declared capability labels — informational only. They describe what the tool touches; the server never enforces them."
```

The shared informational note every badge surface carries: declared, never enforced.


## Badge

```ts
function Badge(props: BadgeProps): Element
```

**Props**

| Prop | Type | Description |
|---|---|---|
| `children` | `ReactNode` | — |
| `variant?` | `string` | — |

**Related:** [BadgeProps](/reference/studio-sdk/components#badgeprops)


## BadgeProps

```ts
interface BadgeProps
```

**Properties**

| Property | Type | Description |
|---|---|---|
| `children` | `ReactNode` | — |
| `variant?` | `string` | — |


## Button

```ts
function Button(props: ButtonActionProps | LinkButtonProps): Element
```

**Parameters**

| Parameter | Type | Default | Description |
|---|---|---|---|
| `props` | `ButtonActionProps \| LinkButtonProps` | — | — |

**Related:** [LinkButtonProps](/reference/studio-sdk/components#linkbuttonprops)


## ButtonProps

```ts
interface ButtonProps extends ButtonVariantProps, ButtonHTMLAttributes<HTMLButtonElement>
```

The ACTION form of `Button`: button attributes, no anchor ones.

This stays an INTERFACE extending `ButtonHTMLAttributes<HTMLButtonElement>`
because it is published plugin API: a plugin may write
`interface MyButton extends ButtonProps {}` (a union cannot be extended —
TS2312) and may hand a `ButtonProps` value to a slot typed as plain
`ButtonHTMLAttributes<HTMLButtonElement>` (a union including anchor attributes
is not assignable — TS2322). The link form is the ADDITIVE `LinkButtonProps`,
and `Button` accepts either.

It declares NO `href` member. `href?: undefined` here would make
`interface MyButton extends ButtonProps { href: string }` a TS2430
("incorrectly extends"), which is a narrowing of a surface that is additive
only. The discriminant `Button` narrows on lives on `ButtonActionProps`.

**Properties**

| Property | Type | Description |
|---|---|---|
| `ref?` | `Ref<HTMLButtonElement>` | A consumer ref for the `<button>`; the prop spread forwards it. |
| `variant?` | `ButtonVariant` | — |

**Related:** [ButtonVariant](/reference/studio-sdk/components#buttonvariant)


## ButtonVariant

```ts
type ButtonVariant = "primary" | "secondary" | "ghost" | "danger"
```


## Card

```ts
function Card(props: CardProps): Element
```

**Props**

| Prop | Type | Description |
|---|---|---|
| `children` | `ReactNode` | — |
| `className?` | `string` | — |
| `interactive?` | `boolean` | Opt in to the hover/focus lift. A card that is not itself an affordance stays flat. |
| `ref?` | `Ref<HTMLDivElement>` | A consumer ref for the card element itself. |
| `style?` | `CSSProperties` | — |

**Related:** [CardProps](/reference/studio-sdk/components#cardprops)


## CardProps

```ts
interface CardProps extends SurfaceProps
```

**Properties**

| Property | Type | Description |
|---|---|---|
| `children` | `ReactNode` | — |
| `className?` | `string` | — |
| `interactive?` | `boolean` | Opt in to the hover/focus lift. A card that is not itself an affordance stays flat. |
| `ref?` | `Ref<HTMLDivElement>` | A consumer ref for the card element itself. |
| `style?` | `CSSProperties` | — |


## CheckCircleIcon

```ts
const CheckCircleIcon: IconComponent
```

Success.

**Related:** [IconComponent](/reference/studio-sdk/components#iconcomponent)


## CheckIcon

```ts
const CheckIcon: IconComponent
```

A bare tick, for selection and confirmation.

**Related:** [IconComponent](/reference/studio-sdk/components#iconcomponent)


## Checkbox

```ts
function Checkbox(props: CheckboxProps): Element
```

**Props**

| Prop | Type | Description |
|---|---|---|
| `aria-label?` | `string` | Accessible name for a checkbox with no visible `label` (e.g. a table cell). An enclosing `Field` wins where both are given — it names the box from visible text, and `aria-label` would override that native label and leave the visible text out of the accessible name (WCAG 2.5.3). |
| `checked?` | `boolean \| "indeterminate"` | `'indeterminate'` is the MIXED state a parent box wears while only some of the children it governs are checked. Radix reports it as `aria-checked="mixed"`; clicking it resolves to checked, which is why the change callback stays boolean. |
| `defaultChecked?` | `boolean` | — |
| `disabled?` | `boolean` | — |
| `label?` | `string` | — |
| `name?` | `string` | — |
| `onCheckedChange?` | `(checked: boolean) => void` | — |
| `value?` | `string` | — |

**Related:** [CheckboxProps](/reference/studio-sdk/components#checkboxprops)


## CheckboxProps

```ts
interface CheckboxProps
```

**Properties**

| Property | Type | Description |
|---|---|---|
| `aria-label?` | `string` | Accessible name for a checkbox with no visible `label` (e.g. a table cell). An enclosing `Field` wins where both are given — it names the box from visible text, and `aria-label` would override that native label and leave the visible text out of the accessible name (WCAG 2.5.3). |
| `checked?` | `boolean \| "indeterminate"` | `'indeterminate'` is the MIXED state a parent box wears while only some of the children it governs are checked. Radix reports it as `aria-checked="mixed"`; clicking it resolves to checked, which is why the change callback stays boolean. |
| `defaultChecked?` | `boolean` | — |
| `disabled?` | `boolean` | — |
| `label?` | `string` | — |
| `name?` | `string` | — |
| `onCheckedChange?` | `(checked: boolean) => void` | — |
| `value?` | `string` | — |


## ChevronDownIcon

```ts
const ChevronDownIcon: IconComponent
```

Expand / collapse affordance.

**Related:** [IconComponent](/reference/studio-sdk/components#iconcomponent)


## ChevronRightIcon

```ts
const ChevronRightIcon: IconComponent
```

Disclosure / breadcrumb separator.

**Related:** [IconComponent](/reference/studio-sdk/components#iconcomponent)


## CloseIcon

```ts
const CloseIcon: IconComponent
```

Dismiss.

**Related:** [IconComponent](/reference/studio-sdk/components#iconcomponent)


## CodeBlock

```ts
function CodeBlock(props: CodeBlockProps): Element
```

**Props**

| Prop | Type | Description |
|---|---|---|
| `code` | `string` | — |
| `language?` | `string` | Optional language label, shown as a caption; purely informational. |

**Related:** [CodeBlockProps](/reference/studio-sdk/components#codeblockprops)


## CodeBlockProps

```ts
interface CodeBlockProps
```

**Properties**

| Property | Type | Description |
|---|---|---|
| `code` | `string` | — |
| `language?` | `string` | Optional language label, shown as a caption; purely informational. |


## CompletionInput

```ts
function CompletionInput(props: CompletionInputProps): ReactNode
```

A completion-backed text input. Suggestions refresh on every edit (a
per-request sequence guard drops a stale response) and render as an ARIA
listbox; selecting an option fills the field and clears the list.

**Props**

| Prop | Type | Description |
|---|---|---|
| `fetchCompletions` | `(value: string) => Promise<readonly string[]>` | Fetch argument completions for the current input value. |
| `onChange` | `(value: string) => void` | — |
| `onError?` | `(error: unknown) => void` | Surfaced (not swallowed) when a completion fetch fails. |
| `placeholder?` | `string` | — |
| `value` | `string` | — |

**Related:** [CompletionInputProps](/reference/studio-sdk/components#completioninputprops)


## CompletionInputProps

```ts
interface CompletionInputProps
```

**Properties**

| Property | Type | Description |
|---|---|---|
| `fetchCompletions` | `(value: string) => Promise<readonly string[]>` | Fetch argument completions for the current input value. |
| `onChange` | `(value: string) => void` | — |
| `onError?` | `(error: unknown) => void` | Surfaced (not swallowed) when a completion fetch fails. |
| `placeholder?` | `string` | — |
| `value` | `string` | — |


## ConfirmDialog

```ts
function ConfirmDialog(props: ConfirmDialogProps): ReactNode
```

**Props**

| Prop | Type | Description |
|---|---|---|
| `children` | `ReactNode` | The prompt body — what the operator is confirming. |
| `confirmLabel` | `string` | The confirm button's resting label (e.g. `Delete scope`). |
| `confirmVariant?` | `"primary" \| "danger"` | — |
| `disabledNote?` | `ReactNode` | A muted node shown in the error slot INSTEAD of the loud `ErrorState`, for the OFF (disabled-feature) case: the action's store is not configured, so the failure is a state, not an error. When set it takes the error slot AND disables the confirm button — the action can only refuse, so there is nothing to retry. |
| `error?` | `string \| Error \| null` | The failed action's error, rendered loudly; omit/`null` while there is none.  A thrown `Error` or the message on its own — `Field`'s `error` is a plain string, and the two props sit side by side on a form, so this one accepts that spelling too rather than making one surface's `error` unassignable to the other's. |
| `isPending` | `boolean` | — |
| `onClose` | `() => void` | — |
| `onConfirm` | `() => void` | — |
| `pendingLabel` | `string` | The Spinner's accessible label while the action runs (e.g. `Deleting`). |
| `title` | `string` | — |

**Related:** [ConfirmDialogProps](/reference/studio-sdk/components#confirmdialogprops)


## ConfirmDialogProps

```ts
interface ConfirmDialogProps
```

**Properties**

| Property | Type | Description |
|---|---|---|
| `children` | `ReactNode` | The prompt body — what the operator is confirming. |
| `confirmLabel` | `string` | The confirm button's resting label (e.g. `Delete scope`). |
| `confirmVariant?` | `"primary" \| "danger"` | — |
| `disabledNote?` | `ReactNode` | A muted node shown in the error slot INSTEAD of the loud `ErrorState`, for the OFF (disabled-feature) case: the action's store is not configured, so the failure is a state, not an error. When set it takes the error slot AND disables the confirm button — the action can only refuse, so there is nothing to retry. |
| `error?` | `string \| Error \| null` | The failed action's error, rendered loudly; omit/`null` while there is none.  A thrown `Error` or the message on its own — `Field`'s `error` is a plain string, and the two props sit side by side on a form, so this one accepts that spelling too rather than making one surface's `error` unassignable to the other's. |
| `isPending` | `boolean` | — |
| `onClose` | `() => void` | — |
| `onConfirm` | `() => void` | — |
| `pendingLabel` | `string` | The Spinner's accessible label while the action runs (e.g. `Deleting`). |
| `title` | `string` | — |


## ConnectorsIcon

```ts
const ConnectorsIcon: IconComponent
```

Connectors: a two-pin power plug on its lead.

**Related:** [IconComponent](/reference/studio-sdk/components#iconcomponent)


## ConversationsIcon

```ts
const ConversationsIcon: IconComponent
```

Conversations: one speech bubble holding transcript lines.

**Related:** [IconComponent](/reference/studio-sdk/components#iconcomponent)


## CopyField

```ts
function CopyField(props: CopyFieldProps): Element
```

**Props**

| Prop | Type | Description |
|---|---|---|
| `caption?` | `string` | — |
| `idPrefix?` | `string` | — |
| `label?` | `string` | — |
| `value` | `string` | — |

**Related:** [CopyFieldProps](/reference/studio-sdk/components#copyfieldprops)


## CopyFieldProps

```ts
interface CopyFieldProps
```

**Properties**

| Property | Type | Description |
|---|---|---|
| `caption?` | `string` | — |
| `idPrefix?` | `string` | — |
| `label?` | `string` | — |
| `value` | `string` | — |


## CopyIcon

```ts
const CopyIcon: IconComponent
```

Copy to clipboard: a sheet duplicated behind another.

**Related:** [IconComponent](/reference/studio-sdk/components#iconcomponent)


## DEFAULT_DATE_RANGE_PRESETS

```ts
const DEFAULT_DATE_RANGE_PRESETS: readonly DateRangePreset[]
```

The presets a picker offers when the caller names none.

**Related:** [DateRangePreset](/reference/studio-sdk/components#daterangepreset)


## DashboardIcon

```ts
const DashboardIcon: IconComponent
```

Observability: a dial gauge — arc, base and needle.

**Related:** [IconComponent](/reference/studio-sdk/components#iconcomponent)


## DateRangePicker

```ts
function DateRangePicker(props: DateRangePickerProps): ReactNode
```

The range control: a row of preset chips over the two `datetime-local` inputs and
an Apply action, with the active window echoed as text below.

Controlled: the caller owns `value` and receives the next window through
`onValueChange` — a preset click emits its token immediately, while the custom
inputs hold a working draft until Apply commits the normalized absolute range.

The drafts track the controlled value, re-seeded on a NEW absolute `value` (keyed
on `from`/`to`), so edits against an unchanged value are preserved but a genuinely
different value replaces an in-progress draft.

**Props**

| Prop | Type | Description |
|---|---|---|
| `aria-label?` | `string` | The control group's accessible name. |
| `disabled?` | `boolean` | — |
| `now?` | `() => Date` | The clock the absolute-range clamp reads. Defaults to the wall clock; a caller (or a test) injects a fixed instant to pin the "no future" boundary. |
| `onValueChange` | `(value: DateRangeValue) => void` | — |
| `presets?` | `readonly DateRangePreset[]` | The preset chips to offer. Defaults to DEFAULT_DATE_RANGE_PRESETS. |
| `value` | `DateRangeValue` | — |

**Related:** [DateRangePickerProps](/reference/studio-sdk/components#daterangepickerprops)


## DateRangePickerProps

```ts
interface DateRangePickerProps
```

**Properties**

| Property | Type | Description |
|---|---|---|
| `aria-label?` | `string` | The control group's accessible name. |
| `disabled?` | `boolean` | — |
| `now?` | `() => Date` | The clock the absolute-range clamp reads. Defaults to the wall clock; a caller (or a test) injects a fixed instant to pin the "no future" boundary. |
| `onValueChange` | `(value: DateRangeValue) => void` | — |
| `presets?` | `readonly DateRangePreset[]` | The preset chips to offer. Defaults to DEFAULT_DATE_RANGE_PRESETS. |
| `value` | `DateRangeValue` | — |

**Related:** [DateRangePreset](/reference/studio-sdk/components#daterangepreset), [DateRangeValue](/reference/studio-sdk/components#daterangevalue)


## DateRangePreset

```ts
interface DateRangePreset
```

A relative preset: a `\d+[hdw]` token the backend resolves, plus its chip label.

**Properties**

| Property | Type | Description |
|---|---|---|
| `label` | `string` | — |
| `token` | `string` | — |


## DateRangeValue

```ts
type DateRangeValue = { kind: "relative"; token: string } | { from: string; kind: "absolute"; to: string }
```

A named time window: either a relative token the backend resolves against its
own clock, or an absolute pair of ISO instants.


## Dialog

```ts
function Dialog(props: DialogProps): Element
```

**Props**

| Prop | Type | Description |
|---|---|---|
| `children?` | `ReactNode` | — |
| `chromeless?` | `boolean` | Drop the forced visual `Title` and the `tai-stack` children wrapper, rendering `children` directly for a chrome-free content mode. The `title` is still required and still names the dialog — it renders visually-hidden — so the accessible name never depends on the visible chrome. |
| `contentClassName?` | `string` | A class merged onto `RadixDialog.Content` after the surface classes, for a host that must reach the content element itself — e.g. a plugin hanging its CSS-scoping root on the panel so its styles apply inside the Radix portal. |
| `defaultOpen?` | `boolean` | — |
| `description?` | `string` | — |
| `dismissable?` | `boolean` | When `false`, the light-dismiss gestures are disabled: Escape and a pointer press outside the panel no longer close it, so the only way out is an explicit action the `children` render (a Done/Cancel button calling `onOpenChange`). The focus trap, the scrim and every other Radix behaviour are unchanged. This is a generic mechanism — it knows nothing of what the dialog holds — for the one-time-reveal case where an accidental dismiss would lose content the server cannot reproduce. Defaults to `true`, the ordinary dismissable modal. This design-system panel renders no built-in close (X) affordance, so there is none to hide here; explicit close stays entirely with the caller's own buttons. |
| `fullscreen?` | `boolean` | Fill the viewport edge to edge instead of centring a fixed-size panel. The overlay, the focus trap and every other Radix behaviour are unchanged. |
| `onOpenChange?` | `(open: boolean) => void` | — |
| `open?` | `boolean` | — |
| `title` | `string` | — |
| `trigger?` | `ReactElement<unknown, string \| JSXElementConstructor<any>>` | A trigger element; when omitted, control the dialog via `open`/`onOpenChange`.  It is a single ELEMENT, not any node: Radix clones its props onto it, so a string throws. The type admits a FRAGMENT — `<></>` is a `ReactElement` — which would render openers carrying neither the `aria-haspopup`/`aria-expanded`/`aria-controls` wiring nor a click handler, so assertSlotElement rejects that one at runtime. |

**Related:** [DialogProps](/reference/studio-sdk/components#dialogprops)


## DialogProps

```ts
interface DialogProps
```

**Properties**

| Property | Type | Description |
|---|---|---|
| `children?` | `ReactNode` | — |
| `chromeless?` | `boolean` | Drop the forced visual `Title` and the `tai-stack` children wrapper, rendering `children` directly for a chrome-free content mode. The `title` is still required and still names the dialog — it renders visually-hidden — so the accessible name never depends on the visible chrome. |
| `contentClassName?` | `string` | A class merged onto `RadixDialog.Content` after the surface classes, for a host that must reach the content element itself — e.g. a plugin hanging its CSS-scoping root on the panel so its styles apply inside the Radix portal. |
| `defaultOpen?` | `boolean` | — |
| `description?` | `string` | — |
| `dismissable?` | `boolean` | When `false`, the light-dismiss gestures are disabled: Escape and a pointer press outside the panel no longer close it, so the only way out is an explicit action the `children` render (a Done/Cancel button calling `onOpenChange`). The focus trap, the scrim and every other Radix behaviour are unchanged. This is a generic mechanism — it knows nothing of what the dialog holds — for the one-time-reveal case where an accidental dismiss would lose content the server cannot reproduce. Defaults to `true`, the ordinary dismissable modal. This design-system panel renders no built-in close (X) affordance, so there is none to hide here; explicit close stays entirely with the caller's own buttons. |
| `fullscreen?` | `boolean` | Fill the viewport edge to edge instead of centring a fixed-size panel. The overlay, the focus trap and every other Radix behaviour are unchanged. |
| `onOpenChange?` | `(open: boolean) => void` | — |
| `open?` | `boolean` | — |
| `title` | `string` | — |
| `trigger?` | `ReactElement<unknown, string \| JSXElementConstructor<any>>` | A trigger element; when omitted, control the dialog via `open`/`onOpenChange`.  It is a single ELEMENT, not any node: Radix clones its props onto it, so a string throws. The type admits a FRAGMENT — `<></>` is a `ReactElement` — which would render openers carrying neither the `aria-haspopup`/`aria-expanded`/`aria-controls` wiring nor a click handler, so assertSlotElement rejects that one at runtime. |


## DirtyGuardBoundary

```ts
function DirtyGuardBoundary(props: { children: ReactNode }): ReactNode
```

Boundary that guards navigation away from a region holding a dirty editor. Editors
inside it arm the guard through `useRegisterDirty`; while any is dirty a route
navigation (a route-token/plugin navigate, an `AppLink`, or a browser
back/forward) confirms through the shared discard dialog, and a full-page unload
raises the native prompt. Use this directly to guard a dirty editor that is a plain
page section (no tabs); `GuardedTabs` wraps this same boundary to additionally
guard tab switches.

**Parameters**

| Parameter | Type | Default | Description |
|---|---|---|---|
| `props` | `{ children: ReactNode }` | — | — |


## Drawer

```ts
function Drawer(props: DrawerProps): Element
```

**Props**

| Prop | Type | Description |
|---|---|---|
| `children` | `ReactNode` | — |
| `defaultOpen?` | `boolean` | — |
| `onOpenChange?` | `(open: boolean) => void` | — |
| `open?` | `boolean` | — |
| `side?` | `"left" \| "right"` | — |
| `title` | `string` | The dialog's accessible name. |
| `trigger?` | `ReactElement<unknown, string \| JSXElementConstructor<any>>` | The opener. Radix labels it and returns focus to it when the drawer closes.  It is a single ELEMENT, not any node: Radix clones its props onto it, so a string throws. The type admits a FRAGMENT — `<></>` is a `ReactElement` — which would render openers carrying neither the `aria-haspopup`/`aria-expanded`/`aria-controls` wiring nor a click handler, so assertSlotElement rejects that one at runtime. |

**Related:** [DrawerProps](/reference/studio-sdk/components#drawerprops)


## DrawerProps

```ts
interface DrawerProps
```

**Properties**

| Property | Type | Description |
|---|---|---|
| `children` | `ReactNode` | — |
| `defaultOpen?` | `boolean` | — |
| `onOpenChange?` | `(open: boolean) => void` | — |
| `open?` | `boolean` | — |
| `side?` | `"left" \| "right"` | — |
| `title` | `string` | The dialog's accessible name. |
| `trigger?` | `ReactElement<unknown, string \| JSXElementConstructor<any>>` | The opener. Radix labels it and returns focus to it when the drawer closes.  It is a single ELEMENT, not any node: Radix clones its props onto it, so a string throws. The type admits a FRAGMENT — `<></>` is a `ReactElement` — which would render openers carrying neither the `aria-haspopup`/`aria-expanded`/`aria-controls` wiring nor a click handler, so assertSlotElement rejects that one at runtime. |


## EmptyState

```ts
function EmptyState(props: EmptyStateProps): Element
```

**Props**

| Prop | Type | Description |
|---|---|---|
| `action?` | `ReactNode` | The guidance line's on-screen action — the control the description names. |
| `className?` | `string` | — |
| `description?` | `string` | — |
| `style?` | `CSSProperties` | — |
| `title` | `string` | — |

**Related:** [EmptyStateProps](/reference/studio-sdk/components#emptystateprops)


## EmptyStateProps

```ts
interface EmptyStateProps extends SurfaceProps
```

**Properties**

| Property | Type | Description |
|---|---|---|
| `action?` | `ReactNode` | The guidance line's on-screen action — the control the description names. |
| `className?` | `string` | — |
| `description?` | `string` | — |
| `style?` | `CSSProperties` | — |
| `title` | `string` | — |


## EntityCardGrid

```ts
function EntityCardGrid(props: EntityCardGridProps): ReactNode
```

**Props**

| Prop | Type | Description |
|---|---|---|
| `aria-label` | `string` | The grid's accessible name (e.g. "Tools"). Rendered as a `list` for AT. |
| `children` | `ReactNode` | — |

**Related:** [EntityCardGridProps](/reference/studio-sdk/components#entitycardgridprops)


## EntityCardGridProps

```ts
interface EntityCardGridProps
```

**Properties**

| Property | Type | Description |
|---|---|---|
| `aria-label` | `string` | The grid's accessible name (e.g. "Tools"). Rendered as a `list` for AT. |
| `children` | `ReactNode` | — |


## ErrorBoundary

```ts
ErrorBoundary
```


## ErrorBoundaryProps

```ts
interface ErrorBoundaryProps
```

**Properties**

| Property | Type | Description |
|---|---|---|
| `children` | `ReactNode` | — |
| `label?` | `string` | Prefixed to the caught error message, naming the contained region. |
| `onError?` | `(error: unknown, info: ErrorInfo) => void` | Called with the caught error and React's component stack, for logging. |


## ErrorState

```ts
function ErrorState(props: ErrorStateProps): Element
```

**Props**

| Prop | Type | Description |
|---|---|---|
| `className?` | `string` | — |
| `message` | `string` | — |
| `onRetry?` | `() => void` | — |
| `style?` | `CSSProperties` | — |

**Related:** [ErrorStateProps](/reference/studio-sdk/components#errorstateprops)


## ErrorStateProps

```ts
interface ErrorStateProps extends SurfaceProps
```

**Properties**

| Property | Type | Description |
|---|---|---|
| `className?` | `string` | — |
| `message` | `string` | — |
| `onRetry?` | `() => void` | — |
| `style?` | `CSSProperties` | — |


## ExplorerColumn

```ts
interface ExplorerColumn
```

One header cell of the table view. `renderRow` must emit exactly this many
 `<TD>`s per item; folder rows span all of them.

**Properties**

| Property | Type | Description |
|---|---|---|
| `header` | `ReactNode` | — |
| `key` | `string` | — |
| `numeric?` | `boolean` | — |


## ExplorerEmptyState

```ts
interface ExplorerEmptyState
```

**Properties**

| Property | Type | Description |
|---|---|---|
| `action?` | `ReactNode` | — |
| `description?` | `string` | — |
| `title` | `string` | — |


## ExplorerEmptyStates

```ts
interface ExplorerEmptyStates
```

The folder-aware empty-state ladder — every label caller-supplied.

**Properties**

| Property | Type | Description |
|---|---|---|
| `empty` | `ExplorerEmptyState` | No items and no folders exist at all. |
| `emptyFolder` | `ExplorerEmptyState` | The current folder holds nothing — no items, no subfolders — yet the  entity set is not globally empty. |
| `noMatch` | `ExplorerEmptyState` | The current folder has items but the active tag/search filter excludes  every one, and it has no subfolders to show. |

**Related:** [ExplorerEmptyState](/reference/studio-sdk/components#exploreremptystate)


## ExplorerSearch

```ts
interface ExplorerSearch<T>
```

Controlled free-text search. `matches` owns the item shape and casing; it is
 called only with a non-empty, already-trimmed query.

**Properties**

| Property | Type | Description |
|---|---|---|
| `label` | `string` | The input's accessible name. |
| `matches` | `(item: T, query: string) => boolean` | — |
| `onChange` | `(value: string) => void` | — |
| `placeholder?` | `string` | — |
| `value` | `string` | — |


## ExplorerTags

```ts
interface ExplorerTags<T>
```

Enables the tag vocabulary, filter chips, and OR filter. Fully controlled.

**Properties**

| Property | Type | Description |
|---|---|---|
| `filterLabel` | `string` | The chip group's accessible name (e.g. "Filter tools by tag"). |
| `getTags` | `(item: T) => readonly string[]` | — |
| `maxVisibleTags?` | `number` | Collapse unselected chips beyond this count into a static "+N more"; unset  shows every chip. Selected chips always show. |
| `onChange` | `(selected: readonly string[]) => void` | — |
| `selected` | `readonly string[]` | — |
| `untaggedLabel` | `string` | The untagged pseudo-tag's visible label (e.g. "Untagged"). |


## ExplorerView

```ts
function ExplorerView<T>(props: ExplorerViewProps<T>): ReactNode
```

**Props**

| Prop | Type | Description |
|---|---|---|
| `columns` | `readonly ExplorerColumn[]` | — |
| `currentFolderId` | `string \| null` | — |
| `emptyStates` | `ExplorerEmptyStates` | — |
| `folders` | `readonly Folder[]` | — |
| `getFolderId` | `(item: T) => string \| null` | The folder an item is filed in (`null` = the root directory). |
| `getItemKey` | `(item: T) => string` | — |
| `items` | `readonly T[]` | — |
| `label` | `string` | The card grid's accessible name; the toggle's is "&lt;label&gt; view". |
| `onNavigate` | `(folderId: string \| null) => void` | — |
| `onOpenItem?` | `(item: T) => void` | Open an item on a pointer click anywhere on its row/card — a convenience  ONLY. The accessible activation path stays the interactive element inside  the cell (the name link), so the row is never a focusable button; a click  originating on a nested control is left to that control. |
| `renderCard` | `(item: T) => ReactNode` | The item's `<Card>` — wrapped in a keyed `listitem` by the explorer. |
| `renderFolderActions?` | `(folder: Folder) => ReactNode` | Per-folder actions rendered on the folder's row/card (e.g. a kebab). |
| `renderRow` | `(item: T) => ReactNode` | The item's table cells (`<TD>`s) — wrapped in a keyed `<TR>` by the explorer. |
| `rootLabel` | `string` | — |
| `search?` | `ExplorerSearch<T>` | — |
| `tags?` | `ExplorerTags<T>` | — |
| `viewSurface` | `string` | Persist + drive the list/card toggle under this surface key. |

**Related:** [ExplorerViewProps](/reference/studio-sdk/components#explorerviewprops)


## ExplorerViewProps

```ts
interface ExplorerViewProps<T>
```

**Properties**

| Property | Type | Description |
|---|---|---|
| `columns` | `readonly ExplorerColumn[]` | — |
| `currentFolderId` | `string \| null` | — |
| `emptyStates` | `ExplorerEmptyStates` | — |
| `folders` | `readonly Folder[]` | — |
| `getFolderId` | `(item: T) => string \| null` | The folder an item is filed in (`null` = the root directory). |
| `getItemKey` | `(item: T) => string` | — |
| `items` | `readonly T[]` | — |
| `label` | `string` | The card grid's accessible name; the toggle's is "&lt;label&gt; view". |
| `onNavigate` | `(folderId: string \| null) => void` | — |
| `onOpenItem?` | `(item: T) => void` | Open an item on a pointer click anywhere on its row/card — a convenience  ONLY. The accessible activation path stays the interactive element inside  the cell (the name link), so the row is never a focusable button; a click  originating on a nested control is left to that control. |
| `renderCard` | `(item: T) => ReactNode` | The item's `<Card>` — wrapped in a keyed `listitem` by the explorer. |
| `renderFolderActions?` | `(folder: Folder) => ReactNode` | Per-folder actions rendered on the folder's row/card (e.g. a kebab). |
| `renderRow` | `(item: T) => ReactNode` | The item's table cells (`<TD>`s) — wrapped in a keyed `<TR>` by the explorer. |
| `rootLabel` | `string` | — |
| `search?` | `ExplorerSearch<T>` | — |
| `tags?` | `ExplorerTags<T>` | — |
| `viewSurface` | `string` | Persist + drive the list/card toggle under this surface key. |

**Related:** [ExplorerColumn](/reference/studio-sdk/components#explorercolumn), [ExplorerEmptyStates](/reference/studio-sdk/components#exploreremptystates), [ExplorerSearch](/reference/studio-sdk/components#explorersearch), [ExplorerTags](/reference/studio-sdk/components#explorertags), [Folder](/reference/studio-sdk/components#folder)


## ExtensionComboBuilder

```ts
function ExtensionComboBuilder(props: ExtensionComboBuilderProps): ReactNode
```

**Props**

| Prop | Type | Description |
|---|---|---|
| `available` | `readonly ({ kind: string; name: string })[]` | The extension catalog the combos are drawn from (caller-fetched). |
| `availableReady?` | `boolean` | Whether the `available` catalog has finished loading. Default `true` (existing consumers keep today's behavior). While `false` the builder reports VALID and suppresses the unknown-name notes, so a stale-seeded combo does not flash invalid before the catalog resolves. |
| `disabled?` | `boolean` | — |
| `idPrefix?` | `string` | — |
| `onChange` | `(next: (string \| { config: z.ZodRecord<z.ZodString, z.ZodUnknown>; name: string })[][]) => void` | Fired with the next list of combos on every add/update/remove. |
| `onValidityChange?` | `(valid: boolean) => void` | Fired (effect-driven) on mount and whenever `value`, `available`, or `availableReady` changes with whether every combo's names are known. A caller blocks submit while this is `false`. The `onChange` payload shape is unchanged; this is a SECOND callback so the prop surface stays backward-compatible. |
| `value` | `readonly (string \| { config: z.ZodRecord<z.ZodString, z.ZodUnknown>; name: string })[][]` | The current list of combos, each an ordered list of extension elements. |

**Related:** [ExtensionComboBuilderProps](/reference/studio-sdk/components#extensioncombobuilderprops)


## ExtensionComboBuilderProps

```ts
interface ExtensionComboBuilderProps
```

**Properties**

| Property | Type | Description |
|---|---|---|
| `available` | `readonly ({ kind: string; name: string })[]` | The extension catalog the combos are drawn from (caller-fetched). |
| `availableReady?` | `boolean` | Whether the `available` catalog has finished loading. Default `true` (existing consumers keep today's behavior). While `false` the builder reports VALID and suppresses the unknown-name notes, so a stale-seeded combo does not flash invalid before the catalog resolves. |
| `disabled?` | `boolean` | — |
| `idPrefix?` | `string` | — |
| `onChange` | `(next: (string \| { config: z.ZodRecord<z.ZodString, z.ZodUnknown>; name: string })[][]) => void` | Fired with the next list of combos on every add/update/remove. |
| `onValidityChange?` | `(valid: boolean) => void` | Fired (effect-driven) on mount and whenever `value`, `available`, or `availableReady` changes with whether every combo's names are known. A caller blocks submit while this is `false`. The `onChange` payload shape is unchanged; this is a SECOND callback so the prop surface stays backward-compatible. |
| `value` | `readonly (string \| { config: z.ZodRecord<z.ZodString, z.ZodUnknown>; name: string })[][]` | The current list of combos, each an ordered list of extension elements. |


## ExtensionFamily

```ts
interface ExtensionFamily
```

One base tool and every extension entry that branches from it.

**Properties**

| Property | Type | Description |
|---|---|---|
| `base` | `string` | The base tool name (the segment before the first underscore). |
| `members` | `readonly ({ kind: string; name: string })[]` | The family's entries, ordered by full name. |


## ExtensionKindGroup

```ts
interface ExtensionKindGroup
```

One extension `kind` and every catalog entry of that kind.

**Properties**

| Property | Type | Description |
|---|---|---|
| `kind` | `string` | The serialized kind value (`wrapper` / `transformer` / `backend`). |
| `members` | `readonly ({ kind: string; name: string })[]` | The kind's entries, ordered by name. |
| `nonStackable` | `boolean` | Whether this kind is non-stackable (single-select in the picker). |


## ExtensionPicker

```ts
function ExtensionPicker(props: ExtensionPickerProps): Element
```

**Props**

| Prop | Type | Description |
|---|---|---|
| `available` | `readonly ({ kind: string; name: string })[]` | The extension catalog (`GET /api/extensions`) the checklist is drawn from. |
| `disabled?` | `boolean` | — |
| `idPrefix?` | `string` | — |
| `onChange` | `(names: string[]) => void` | Fired with the next ordered selection whenever a box toggles. |
| `value` | `readonly string[]` | The currently-selected extension names, in combo (stacking) order. |

**Related:** [ExtensionPickerProps](/reference/studio-sdk/components#extensionpickerprops)


## ExtensionPickerProps

```ts
interface ExtensionPickerProps
```

**Properties**

| Property | Type | Description |
|---|---|---|
| `available` | `readonly ({ kind: string; name: string })[]` | The extension catalog (`GET /api/extensions`) the checklist is drawn from. |
| `disabled?` | `boolean` | — |
| `idPrefix?` | `string` | — |
| `onChange` | `(names: string[]) => void` | Fired with the next ordered selection whenever a box toggles. |
| `value` | `readonly string[]` | The currently-selected extension names, in combo (stacking) order. |


## ExtensionsIcon

```ts
const ExtensionsIcon: IconComponent
```

Extensions: a jigsaw piece — knob above, socket to the right.

**Related:** [IconComponent](/reference/studio-sdk/components#iconcomponent)


## ExternalLinkButton

```ts
function ExternalLinkButton(props: ExternalLinkButtonProps): Element
```

**Props**

| Prop | Type | Description |
|---|---|---|
| `children?` | `ReactNode` | — |
| `url` | `string` | — |

**Related:** [ExternalLinkButtonProps](/reference/studio-sdk/components#externallinkbuttonprops)


## ExternalLinkButtonProps

```ts
interface ExternalLinkButtonProps
```

**Properties**

| Property | Type | Description |
|---|---|---|
| `children?` | `ReactNode` | — |
| `url` | `string` | — |


## ExternalLinkIcon

```ts
const ExternalLinkIcon: IconComponent
```

Opens in a new tab / leaves the app.

**Related:** [IconComponent](/reference/studio-sdk/components#iconcomponent)


## EyeIcon

```ts
const EyeIcon: IconComponent
```

Reveal a masked value.

**Related:** [IconComponent](/reference/studio-sdk/components#iconcomponent)


## EyeOffIcon

```ts
const EyeOffIcon: IconComponent
```

Mask a revealed value.

**Related:** [IconComponent](/reference/studio-sdk/components#iconcomponent)


## Field

```ts
function Field(props: FieldProps): Element
```

**Props**

| Prop | Type | Description |
|---|---|---|
| `children` | `ReactNode` | — |
| `className?` | `string` | APPENDED to `tai-field` rather than replacing it, so the field keeps its paint. |
| `description?` | `string` | — |
| `error?` | `string` | — |
| `group?` | `boolean` | Set when the wrapped control is a GROUP rather than one labelable element — a `RadioGroup`, a set of checkboxes, any `role="radiogroup"`/`role="group"` panel.  `<label for>` can only name a labelable element: pointed at a group it either dangles at an id no element carries or names an element the browser refuses to label, and it is inert either way. So a group Field wraps `children` in the group CONTAINER itself — a `<div role="group">` carrying `aria-labelledby` at the label and `aria-describedby` at the description and error — and the label becomes a `<span>`, because a `<label>` associated with nothing is a semantics lie.  IT ALSO CHANGES WHAT A CHILD READS FROM useFieldControl. The container is what carries the name and the description, so the group Field publishes NEITHER `id` NOR `aria-describedby` — both are `undefined`, and a control that spread them would take the group's own name and description for itself. `aria-invalid` still crosses: `role="group"` does not support it (ARIA 1.2), so the control is the only place an errored group can expose its invalid state.  Naming therefore happens BY CONSTRUCTION rather than by the call site remembering to read a hook. The cost is one extra container announcement where the child has a group role of its own; that is accepted deliberately, because the two failure directions are asymmetric — a redundant container is audible, an unnamed group is silent.  A group child left UNMARKED is a build failure, not a runtime surprise: `components/field-group.test.ts` ("Field group contract" → "marks every Field whose child does not claim its control id") scans every Field call site in the repository, and a child outside its list of control-id-claiming components — `RadioGroup` among them — reddens until the site passes `group`. |
| `hideLabel?` | `boolean` | Render the label for ASSISTIVE TECH ONLY — visually hidden, but still the control's accessible name through the very same `<label for>` (or, in a group, the same `aria-labelledby`). The label element stays in the DOM behind `tai-visually-hidden`, so nothing about the accessible name changes; only its paint is dropped. It is the standard idiom for a dense grid of otherwise identical fields, where each row needs a UNIQUE, addressable name (a voice-control user can only say a name that exists) but there is no room for a visible label on every row — the same trade `RadioGroup`'s `visuallyHiddenLabel` and `ViewToggle` make. The label text still governs WCAG 2.5.3 (Label in Name), so a caller passes the real per-row name as `label`, not a decoration. |
| `label` | `string` | — |
| `style?` | `CSSProperties` | — |

**Related:** [FieldProps](/reference/studio-sdk/components#fieldprops)


## FieldControlProps

```ts
interface FieldControlProps
```

The a11y wiring a form control spreads onto its element.

**Properties**

| Property | Type | Description |
|---|---|---|
| `aria-describedby` | `string \| undefined` | — |
| `aria-invalid` | `true \| undefined` | — |
| `id` | `string \| undefined` | — |


## FieldProps

```ts
interface FieldProps
```

**Properties**

| Property | Type | Description |
|---|---|---|
| `children` | `ReactNode` | — |
| `className?` | `string` | APPENDED to `tai-field` rather than replacing it, so the field keeps its paint. |
| `description?` | `string` | — |
| `error?` | `string` | — |
| `group?` | `boolean` | Set when the wrapped control is a GROUP rather than one labelable element — a `RadioGroup`, a set of checkboxes, any `role="radiogroup"`/`role="group"` panel.  `<label for>` can only name a labelable element: pointed at a group it either dangles at an id no element carries or names an element the browser refuses to label, and it is inert either way. So a group Field wraps `children` in the group CONTAINER itself — a `<div role="group">` carrying `aria-labelledby` at the label and `aria-describedby` at the description and error — and the label becomes a `<span>`, because a `<label>` associated with nothing is a semantics lie.  IT ALSO CHANGES WHAT A CHILD READS FROM useFieldControl. The container is what carries the name and the description, so the group Field publishes NEITHER `id` NOR `aria-describedby` — both are `undefined`, and a control that spread them would take the group's own name and description for itself. `aria-invalid` still crosses: `role="group"` does not support it (ARIA 1.2), so the control is the only place an errored group can expose its invalid state.  Naming therefore happens BY CONSTRUCTION rather than by the call site remembering to read a hook. The cost is one extra container announcement where the child has a group role of its own; that is accepted deliberately, because the two failure directions are asymmetric — a redundant container is audible, an unnamed group is silent.  A group child left UNMARKED is a build failure, not a runtime surprise: `components/field-group.test.ts` ("Field group contract" → "marks every Field whose child does not claim its control id") scans every Field call site in the repository, and a child outside its list of control-id-claiming components — `RadioGroup` among them — reddens until the site passes `group`. |
| `hideLabel?` | `boolean` | Render the label for ASSISTIVE TECH ONLY — visually hidden, but still the control's accessible name through the very same `<label for>` (or, in a group, the same `aria-labelledby`). The label element stays in the DOM behind `tai-visually-hidden`, so nothing about the accessible name changes; only its paint is dropped. It is the standard idiom for a dense grid of otherwise identical fields, where each row needs a UNIQUE, addressable name (a voice-control user can only say a name that exists) but there is no room for a visible label on every row — the same trade `RadioGroup`'s `visuallyHiddenLabel` and `ViewToggle` make. The label text still governs WCAG 2.5.3 (Label in Name), so a caller passes the real per-row name as `label`, not a decoration. |
| `label` | `string` | — |
| `style?` | `CSSProperties` | — |


## FilterIcon

```ts
const FilterIcon: IconComponent
```

A funnel.

**Related:** [IconComponent](/reference/studio-sdk/components#iconcomponent)


## FleetReport

```ts
function FleetReport(props: FleetReportProps): ReactNode
```

**Props**

| Prop | Type | Description |
|---|---|---|
| `action?` | `"reload" \| "save" \| "deregister" \| "remove"` | Whose framing the copy takes: a saved config change (`'save'`), a dispatched reload (`'reload'`), a connector detach fan-out (`'deregister'`), or a live-registry tool removal (`'remove'`). All but `'save'` drop the "Change saved" framing; `'deregister'` and `'remove'` also point remediation back at re-running that same detach, since a reload would re-attach the detached server or tool. |
| `summary` | `FleetReportSummary \| null` | — |

**Related:** [FleetReportProps](/reference/studio-sdk/components#fleetreportprops)


## FleetReportProps

```ts
interface FleetReportProps
```

Render a fleet broadcast's failure state. Returns `null` for a converged/absent
report (nothing to warn about) so callers can always mount it unconditionally
beside their own success line.

**Properties**

| Property | Type | Description |
|---|---|---|
| `action?` | `"reload" \| "save" \| "deregister" \| "remove"` | Whose framing the copy takes: a saved config change (`'save'`), a dispatched reload (`'reload'`), a connector detach fan-out (`'deregister'`), or a live-registry tool removal (`'remove'`). All but `'save'` drop the "Change saved" framing; `'deregister'` and `'remove'` also point remediation back at re-running that same detach, since a reload would re-attach the detached server or tool. |
| `summary` | `FleetReportSummary \| null` | — |


## Folder

```ts
interface Folder
```

A folder in the tree, in the SDK's camelCase UI shape (`parentId`, `null` = root).

**Properties**

| Property | Type | Description |
|---|---|---|
| `id` | `string` | — |
| `name` | `string` | — |
| `parentId` | `string \| null` | — |


## FolderBreadcrumb

```ts
function FolderBreadcrumb(props: FolderBreadcrumbProps): ReactNode
```

The root → … → current path; each crumb navigates to that level.

**Props**

| Prop | Type | Description |
|---|---|---|
| `currentFolderId` | `string \| null` | — |
| `folders` | `readonly Folder[]` | — |
| `onNavigate` | `(folderId: string \| null) => void` | — |
| `rootLabel` | `string` | The root crumb's label (e.g. "All tools"). |

**Related:** [FolderBreadcrumbProps](/reference/studio-sdk/components#folderbreadcrumbprops)


## FolderBreadcrumbProps

```ts
interface FolderBreadcrumbProps
```

**Properties**

| Property | Type | Description |
|---|---|---|
| `currentFolderId` | `string \| null` | — |
| `folders` | `readonly Folder[]` | — |
| `onNavigate` | `(folderId: string \| null) => void` | — |
| `rootLabel` | `string` | The root crumb's label (e.g. "All tools"). |

**Related:** [Folder](/reference/studio-sdk/components#folder)


## FolderIcon

```ts
const FolderIcon: IconComponent
```

Folder: a tabbed folder, for the tool-organization tree and breadcrumbs.

**Related:** [IconComponent](/reference/studio-sdk/components#iconcomponent)


## FolderPicker

```ts
function FolderPicker(props: FolderPickerProps): ReactNode
```

A folder chooser with inline new-folder creation, for the edit / move dialogs.

**Props**

| Prop | Type | Description |
|---|---|---|
| `disabled?` | `boolean` | — |
| `folders` | `readonly Folder[]` | — |
| `onChange` | `(folderId: string \| null) => void` | — |
| `onCreateFolder` | `(name: string, parentId: string \| null) => Promise<string>` | Create a folder under `parentId` (the current selection) and resolve with its id; the picker selects it. Rejection is the caller's to surface — the picker leaves the draft in place so the operator can retry. |
| `unfiledLabel` | `string` | The unfiled option's label (e.g. "No folder"). |
| `value` | `string \| null` | The chosen folder id, or `null` for unfiled. |

**Related:** [FolderPickerProps](/reference/studio-sdk/components#folderpickerprops)


## FolderPickerProps

```ts
interface FolderPickerProps
```

**Properties**

| Property | Type | Description |
|---|---|---|
| `disabled?` | `boolean` | — |
| `folders` | `readonly Folder[]` | — |
| `onChange` | `(folderId: string \| null) => void` | — |
| `onCreateFolder` | `(name: string, parentId: string \| null) => Promise<string>` | Create a folder under `parentId` (the current selection) and resolve with its id; the picker selects it. Rejection is the caller's to surface — the picker leaves the draft in place so the operator can retry. |
| `unfiledLabel` | `string` | The unfiled option's label (e.g. "No folder"). |
| `value` | `string \| null` | The chosen folder id, or `null` for unfiled. |

**Related:** [Folder](/reference/studio-sdk/components#folder)


## FolderRow

```ts
function FolderRow(props: FolderRowProps): ReactNode
```

One subfolder in the current-directory explorer; opening it navigates inward.

**Props**

| Prop | Type | Description |
|---|---|---|
| `folder` | `Folder` | — |
| `onOpen` | `(folderId: string) => void` | — |

**Related:** [FolderRowProps](/reference/studio-sdk/components#folderrowprops)


## FolderRowProps

```ts
interface FolderRowProps
```

**Properties**

| Property | Type | Description |
|---|---|---|
| `folder` | `Folder` | — |
| `onOpen` | `(folderId: string) => void` | — |

**Related:** [Folder](/reference/studio-sdk/components#folder)


## FormDialog

```ts
function FormDialog(props: FormDialogProps): ReactNode
```

**Props**

| Prop | Type | Description |
|---|---|---|
| `cancelLabel?` | `string` | The Cancel button's label. |
| `children` | `ReactNode` | The form body — the caller's fields. |
| `description?` | `string` | — |
| `onClose` | `() => void` | Called on a close gesture (Cancel, Escape, overlay) and on a successful submit — at most once per dialog lifecycle. A close landing mid-submit wins: the later resolution does not fire a second `onClose`. |
| `onSubmit` | `() => void \| Promise<void>` | The action the form performs. A returned promise is awaited: the dialog closes only when it RESOLVES; a rejection (or a synchronous throw) keeps the dialog open and renders the failure loudly. A `void` return closes the dialog synchronously. |
| `pendingLabel` | `string` | The Spinner's accessible label while the action runs (e.g. `Creating`). |
| `submitDisabled?` | `boolean` | Blocks submission by click and by Enter while incomplete (a disabled default button is not an implicit-submission target). |
| `submitLabel` | `string` | The submit button's resting label (e.g. `Create scope`). |
| `submitVariant?` | `"primary" \| "danger"` | — |
| `title` | `string` | — |

**Related:** [FormDialogProps](/reference/studio-sdk/components#formdialogprops)


## FormDialogProps

```ts
interface FormDialogProps
```

**Properties**

| Property | Type | Description |
|---|---|---|
| `cancelLabel?` | `string` | The Cancel button's label. |
| `children` | `ReactNode` | The form body — the caller's fields. |
| `description?` | `string` | — |
| `onClose` | `() => void` | Called on a close gesture (Cancel, Escape, overlay) and on a successful submit — at most once per dialog lifecycle. A close landing mid-submit wins: the later resolution does not fire a second `onClose`. |
| `onSubmit` | `() => void \| Promise<void>` | The action the form performs. A returned promise is awaited: the dialog closes only when it RESOLVES; a rejection (or a synchronous throw) keeps the dialog open and renders the failure loudly. A `void` return closes the dialog synchronously. |
| `pendingLabel` | `string` | The Spinner's accessible label while the action runs (e.g. `Creating`). |
| `submitDisabled?` | `boolean` | Blocks submission by click and by Enter while incomplete (a disabled default button is not an implicit-submission target). |
| `submitLabel` | `string` | The submit button's resting label (e.g. `Create scope`). |
| `submitVariant?` | `"primary" \| "danger"` | — |
| `title` | `string` | — |


## GridIcon

```ts
const GridIcon: IconComponent
```

Grid: a 2x2 tile array — the card-view half of the list/card toggle.

**Related:** [IconComponent](/reference/studio-sdk/components#iconcomponent)


## GuardedTabs

```ts
function GuardedTabs(props: { defaultValue?: string; items: readonly TabItem[] }): ReactNode
```

A drop-in replacement for `Tabs` that guards a switch away from a dirty
editor. Wraps the tabs in a `DirtyGuardBoundary`, so an editor rendered in
any panel can arm the guard through `useRegisterDirty`.

**Parameters**

| Parameter | Type | Default | Description |
|---|---|---|---|
| `props` | `{ defaultValue?: string; items: readonly TabItem[] }` | — | — |

**Related:** [TabItem](/reference/studio-sdk/components#tabitem)


## HeadingLevel

```ts
type HeadingLevel = 1 | 2 | 3 | 4 | 5 | 6
```

The heading levels ATX syntax can express, `#` through `######`.


## HooksIcon

```ts
const HooksIcon: IconComponent
```

Hooks: a webhook fan-out — one source branching to two subscribers.

**Related:** [IconComponent](/reference/studio-sdk/components#iconcomponent)


## IconComponent

```ts
type IconComponent = (props: IconProps) => ReactElement
```

The shape every exported icon satisfies.

**Related:** [IconProps](/reference/studio-sdk/components#iconprops)


## IconProps

```ts
type IconProps = SVGProps<SVGSVGElement>
```

Props of every icon: the full SVG surface, so `style`, events and ARIA pass
through. `className` is APPENDED to the `tai-icon` sizing class rather than
replacing it — the same merge every other design-system primitive does.


## InteractionsIcon

```ts
const InteractionsIcon: IconComponent
```

Interactions: two overlapping speech bubbles — a conversation.

**Related:** [IconComponent](/reference/studio-sdk/components#iconcomponent)


## JsonDiff

```ts
function JsonDiff(props: JsonDiffProps): ReactNode
```

**Props**

| Prop | Type | Description |
|---|---|---|
| `after` | `unknown` | — |
| `before` | `unknown` | — |

**Related:** [JsonDiffProps](/reference/studio-sdk/components#jsondiffprops)


## JsonDiffProps

```ts
interface JsonDiffProps
```

**Properties**

| Property | Type | Description |
|---|---|---|
| `after` | `unknown` | — |
| `before` | `unknown` | — |


## JsonDiffRow

```ts
interface JsonDiffRow
```

One leaf difference between the two JSON values.

**Properties**

| Property | Type | Description |
|---|---|---|
| `after?` | `unknown` | The value on the RIGHT side — set for `added` and `changed` rows. |
| `before?` | `unknown` | The value on the LEFT side — set for `removed` and `changed` rows. |
| `kind` | `"added" \| "removed" \| "changed"` | — |
| `path` | `string` | The dotted path to the differing key, e.g. `fixed_kwargs.city` (`''` = root). |


## JsonTree

```ts
function JsonTree(props: JsonTreeProps): ReactElement
```

**Props**

| Prop | Type | Description |
|---|---|---|
| `data` | `unknown` | — |
| `defaultExpanded?` | `boolean` | Opts the whole tree out of the depth-guarded default: `true` expands every node (up to the depth cap), `false` collapses to the root. Omitted, the tree opens through AUTO_EXPAND_DEPTH and collapses everything below. |
| `label?` | `string` | The region's accessible name, applied only while the pane actually scrolls. |

**Related:** [JsonTreeProps](/reference/studio-sdk/components#jsontreeprops)


## JsonTreeProps

```ts
interface JsonTreeProps
```

**Properties**

| Property | Type | Description |
|---|---|---|
| `data` | `unknown` | — |
| `defaultExpanded?` | `boolean` | Opts the whole tree out of the depth-guarded default: `true` expands every node (up to the depth cap), `false` collapses to the root. Omitted, the tree opens through AUTO_EXPAND_DEPTH and collapses everything below. |
| `label?` | `string` | The region's accessible name, applied only while the pane actually scrolls. |


## LinkButtonProps

```ts
interface LinkButtonProps extends ButtonVariantProps, AnchorHTMLAttributes<HTMLAnchorElement>
```

The LINK form of `Button`: an `href`, so it carries anchor attributes and no
button ones.

`target` and `rel` are inherited from `AnchorHTMLAttributes` and are settable,
but on the EXTERNAL branch they are not the caller's to choose: an absolute
http(s) URL always renders `target="_blank"` with
`rel="noopener noreferrer external"`, whatever the caller passed. On the
in-app branch `target` is the caller's and `rel` is pinned only when they ask
for `_blank`.

It declares NO `ref`, and that is a REFUSAL rather than an omission: a blocked
href renders a `NeutralizedLink` span and no anchor at all, so a
`Ref<HTMLAnchorElement>` would be filled on two of the three renderings and
left null on the third — a ref that silently does nothing for exactly the
input the check exists to catch. A caller that needs the element wraps the
button.

**Properties**

| Property | Type | Description |
|---|---|---|
| `href` | `string` | — |
| `variant?` | `ButtonVariant` | — |

**Related:** [ButtonVariant](/reference/studio-sdk/components#buttonvariant)


## ManifestIcon

```ts
const ManifestIcon: IconComponent
```

Manifest: a document with a folded corner and its entries.

**Related:** [IconComponent](/reference/studio-sdk/components#iconcomponent)


## Markdown

```ts
function Markdown(props: MarkdownProps): Element
```

**Props**

| Prop | Type | Description |
|---|---|---|
| `className?` | `string` | Appended to `tai-prose` so a caller can position the block without losing its paint. |
| `markdown` | `string` | The markdown source. Rendered as an escaped-by-construction React tree. |

**Related:** [MarkdownProps](/reference/studio-sdk/components#markdownprops)


## MarkdownBlock

```ts
type MarkdownBlock = { level: HeadingLevel; text: string; type: "heading" } | { text: string; type: "paragraph" } | { code: string; language: string | undefined; type: "code" } | { items: readonly string[]; ordered: boolean; start?: number; type: "list" } | { children: readonly MarkdownBlock[]; type: "blockquote" } | { type: "thematicBreak" }
```

A parsed block. Inline markup inside `text` is resolved at render time.

**Related:** [HeadingLevel](/reference/studio-sdk/components#headinglevel)


## MarkdownProps

```ts
interface MarkdownProps
```

**Properties**

| Property | Type | Description |
|---|---|---|
| `className?` | `string` | Appended to `tai-prose` so a caller can position the block without losing its paint. |
| `markdown` | `string` | The markdown source. Rendered as an escaped-by-construction React tree. |


## MarketplaceIcon

```ts
const MarketplaceIcon: IconComponent
```

Marketplace: a shopping bag.

**Related:** [IconComponent](/reference/studio-sdk/components#iconcomponent)


## MenuIcon

```ts
const MenuIcon: IconComponent
```

The three-bar menu / drawer toggle.

**Related:** [IconComponent](/reference/studio-sdk/components#iconcomponent)


## MinusIcon

```ts
const MinusIcon: IconComponent
```

A bare dash, for a partial selection — the tick's mixed-state counterpart.

**Related:** [IconComponent](/reference/studio-sdk/components#iconcomponent)


## MonitorIcon

```ts
const MonitorIcon: IconComponent
```

Follow the operating system.

**Related:** [IconComponent](/reference/studio-sdk/components#iconcomponent)


## MoonIcon

```ts
const MoonIcon: IconComponent
```

Dark theme.

**Related:** [IconComponent](/reference/studio-sdk/components#iconcomponent)


## NAV_ICONS

```ts
const NAV_ICONS: Readonly<Record<Exclude<RouteToken, "login">, IconComponent>>
```

The canonical mark for every navigable route token. `login` is excluded: it is
not a destination the navigation surfaces list. Keyed by the token type, so
adding a route to `RouteToken` fails the build here until it gets a mark.

**Related:** [IconComponent](/reference/studio-sdk/components#iconcomponent), [RouteToken](/reference/studio-sdk/navigation#routetoken)


## NON_STACKABLE_KIND

```ts
const NON_STACKABLE_KIND: "backend"
```

The single non-stackable extension kind: at most one `backend` extension may
appear in a combo (the `backend` kind is declared non-stackable, `multiple=False`).
The `ExtensionPicker` enforces single-select for this kind.


## NotificationsIcon

```ts
const NotificationsIcon: IconComponent
```

Notifications: a bell with its clapper.

**Related:** [IconComponent](/reference/studio-sdk/components#iconcomponent)


## NumberInput

```ts
function NumberInput(props: NumberInputProps): Element
```

**Parameters**

| Parameter | Type | Default | Description |
|---|---|---|---|
| `props` | `NumberInputProps` | — | — |

**Related:** [NumberInputProps](/reference/studio-sdk/components#numberinputprops)


## NumberInputProps

```ts
type NumberInputProps = Omit<InputHTMLAttributes<HTMLInputElement>, "type"> & ControlRefProps<HTMLInputElement>
```


## NumericColumnProps

```ts
interface NumericColumnProps
```

Right-align a column of digits on tabular figures.

**Properties**

| Property | Type | Description |
|---|---|---|
| `numeric?` | `boolean` | — |


## OpenTargetOptions

```ts
interface OpenTargetOptions
```

**Properties**

| Property | Type | Description |
|---|---|---|
| `ignoreWithin?` | `string` | Extra selector(s) that also count as "nested interactive" for THIS surface,  merged with the built-in set. Use it for a marked non-`button`/`a` subtree  that owns its own clicks (e.g. an expandable preview keyed by a data  attribute). Standard CSS selector syntax, comma-separated. |
| `keyboard?` | `boolean` | Make the entry itself keyboard-operable: a `tabIndex: 0` focus stop with  Enter/Space activation. Leave off when a nested interactive element already  carries the accessible path (see the keyboard note above) — adding it there  would create a duplicate tab stop. |
| `onOpen` | `() => void \| undefined` | Open the entry. `undefined` disables the affordance entirely — the helper  returns empty props so the element stays inert (no cursor, no handlers),  which lets a caller wire the whole thing behind an optional prop. |


## OpenTargetProps

```ts
interface OpenTargetProps
```

The React attributes `openTargetProps` yields — spread onto the entry
 element (`<TR>`, card `<div>`). Every field is optional so the disabled case
 is the empty object.

**Properties**

| Property | Type | Description |
|---|---|---|
| `onClick?` | `(event: MouseEvent<HTMLElement>) => void` | — |
| `onKeyDown?` | `(event: KeyboardEvent<HTMLElement>) => void` | — |
| `style?` | `{ cursor: "pointer" }` | — |
| `tabIndex?` | `number` | — |


## OverflowRegionAttributes

```ts
interface OverflowRegionAttributes
```

The attribute set a scrolling box wears; every value is absent while it fits.

**Properties**

| Property | Type | Description |
|---|---|---|
| `aria-label?` | `string` | — |
| `ref` | `RefCallback<HTMLElement>` | Attach to the scrolling element; it is what the measurement follows. |
| `role?` | `"region"` | — |
| `tabIndex?` | `0` | — |


## OverlayDetails

```ts
interface OverlayDetails
```

The two overlay fields this control edits.

**Properties**

| Property | Type | Description |
|---|---|---|
| `displayName` | `string` | The raw display-name draft (unblank-normalized only at write time). |
| `tags` | `readonly string[]` | — |


## OverlayDetailsFields

```ts
function OverlayDetailsFields(props: OverlayDetailsFieldsProps): ReactNode
```

**Props**

| Prop | Type | Description |
|---|---|---|
| `disabled?` | `boolean` | — |
| `namePlaceholder?` | `string` | The tool's real name, shown as the display-name input's placeholder so an empty override reads as "falls back to &lt;name&gt;". |
| `nativeTags?` | `readonly string[]` | Read-only plugin-native tags, rendered as distinct chips. Present only in the edit surfaces that want native and overlay tags shown apart. |
| `onChange` | `(next: OverlayDetails) => void` | — |
| `value` | `OverlayDetails` | — |

**Related:** [OverlayDetailsFieldsProps](/reference/studio-sdk/components#overlaydetailsfieldsprops)


## OverlayDetailsFieldsProps

```ts
interface OverlayDetailsFieldsProps
```

**Properties**

| Property | Type | Description |
|---|---|---|
| `disabled?` | `boolean` | — |
| `namePlaceholder?` | `string` | The tool's real name, shown as the display-name input's placeholder so an empty override reads as "falls back to &lt;name&gt;". |
| `nativeTags?` | `readonly string[]` | Read-only plugin-native tags, rendered as distinct chips. Present only in the edit surfaces that want native and overlay tags shown apart. |
| `onChange` | `(next: OverlayDetails) => void` | — |
| `value` | `OverlayDetails` | — |

**Related:** [OverlayDetails](/reference/studio-sdk/components#overlaydetails)


## Page

```ts
function Page(props: PageLayoutProps): ReactElement
```

The screen wrapper: the max-width, the gutters, and the vertical rhythm.

**Props**

| Prop | Type | Description |
|---|---|---|
| `children` | `ReactNode` | — |
| `className?` | `string` | — |
| `ref?` | `Ref<HTMLDivElement>` | A consumer ref for the wrapper element itself. |
| `style?` | `CSSProperties` | — |

**Related:** [PageLayoutProps](/reference/studio-sdk/components#pagelayoutprops)


## PageFillProvider

```ts
function PageFillProvider(props: { children: ReactNode }): ReactNode
```

Wraps the shell's routed region: holds the count of pages asking for fill mode
and publishes both the registrar (for `useFillViewport`) and the active
flag (for `usePageFillActive`). Renders no DOM of its own.

**Parameters**

| Parameter | Type | Default | Description |
|---|---|---|---|
| `props` | `{ children: ReactNode }` | — | — |


## PageHeader

```ts
function PageHeader(props: PageHeaderProps): ReactElement
```

**Props**

| Prop | Type | Description |
|---|---|---|
| `actions?` | `ReactNode` | — |
| `description?` | `string` | — |
| `eyebrow?` | `string` | The screen's existing nav-section label, rendered ABOVE the h1 — never inside it. |
| `id?` | `string` | An id for the `<h1>`, not for the `<header>` root. The heading is the element a caller has a reason to address — an `aria-labelledby` pointing at it names the region from the screen's own title — and it is the other way to reach it as a focus target. |
| `title` | `string` | The screen's title. It is the `<h1>`'s accessible name, VERBATIM. |
| `titleRef?` | `Ref<HTMLHeadingElement>` | A consumer ref for the `<h1>`. Published so a caller that wants to move focus to the new screen's title — after a route change, say — has the element without querying for it; the heading is focusable for that reason. |

**Related:** [PageHeaderProps](/reference/studio-sdk/components#pageheaderprops)


## PageHeaderProps

```ts
interface PageHeaderProps
```

**Properties**

| Property | Type | Description |
|---|---|---|
| `actions?` | `ReactNode` | — |
| `description?` | `string` | — |
| `eyebrow?` | `string` | The screen's existing nav-section label, rendered ABOVE the h1 — never inside it. |
| `id?` | `string` | An id for the `<h1>`, not for the `<header>` root. The heading is the element a caller has a reason to address — an `aria-labelledby` pointing at it names the region from the screen's own title — and it is the other way to reach it as a focus target. |
| `title` | `string` | The screen's title. It is the `<h1>`'s accessible name, VERBATIM. |
| `titleRef?` | `Ref<HTMLHeadingElement>` | A consumer ref for the `<h1>`. Published so a caller that wants to move focus to the new screen's title — after a route change, say — has the element without querying for it; the heading is focusable for that reason. |


## PageLayoutProps

```ts
interface PageLayoutProps extends LayoutElementProps
```

**Properties**

| Property | Type | Description |
|---|---|---|
| `children` | `ReactNode` | — |
| `className?` | `string` | — |
| `ref?` | `Ref<HTMLDivElement>` | A consumer ref for the wrapper element itself. |
| `style?` | `CSSProperties` | — |


## PendingIcon

```ts
const PendingIcon: IconComponent
```

Pending / running / queued — a clock hand inside a dashed ring.

The dash period has to TILE the circumference or the ring closes on a seam.
At r = 9 the circumference is 2π·9 = 56.5487. A 3.2/3.2 array (period 6.4)
fits 8.84 times, so the ring shut with a 2.15 gap instead of 3.2 — and with
the inherited round linecap eating 1.6 at each end that seam rendered ~0.37 px
wide at 16 px, reading as joined while every other gap read as a gap. Eight
whole periods divide it exactly: 56.5487 / 8 = 7.0686, halved for the dash and
the gap = 3.5343 each, giving 16 even segments and no seam.

**Related:** [IconComponent](/reference/studio-sdk/components#iconcomponent)


## PluginIcon

```ts
const PluginIcon: IconComponent
```

Plugin: a puzzle piece — the host's provenance mark for plugin-contributed nav.

**Related:** [IconComponent](/reference/studio-sdk/components#iconcomponent)


## PresetsIcon

```ts
const PresetsIcon: IconComponent
```

Presets: three sliders, each with its knob at a different stop.

**Related:** [IconComponent](/reference/studio-sdk/components#iconcomponent)


## ProgressBar

```ts
function ProgressBar(props: ProgressBarProps): ReactNode
```

Render a progress bar. With a positive `total`, `value/total` fills the track
and the percentage is announced via `aria-valuenow`; without one, the bar is
indeterminate (no `aria-valuenow`, per ARIA).

**Props**

| Prop | Type | Description |
|---|---|---|
| `message?` | `string` | Optional human-readable status shown alongside the bar. |
| `total?` | `number` | Upper bound. When absent or non-positive the bar is INDETERMINATE. |
| `value?` | `number` | Current progress. Clamped to `[0, total]` when `total` is known. |

**Related:** [ProgressBarProps](/reference/studio-sdk/components#progressbarprops)


## ProgressBarProps

```ts
interface ProgressBarProps
```

**Properties**

| Property | Type | Description |
|---|---|---|
| `message?` | `string` | Optional human-readable status shown alongside the bar. |
| `total?` | `number` | Upper bound. When absent or non-positive the bar is INDETERMINATE. |
| `value?` | `number` | Current progress. Clamped to `[0, total]` when `total` is known. |


## ProseScrollLabels

```ts
interface ProseScrollLabels
```

The fallback name per instrumented surface, when no heading precedes it.

**Properties**

| Property | Type | Description |
|---|---|---|
| `pre?` | `string` | — |
| `table?` | `string` | — |


## RadioGroup

```ts
function RadioGroup(props: RadioGroupProps): Element
```

**Props**

| Prop | Type | Description |
|---|---|---|
| `aria-label?` | `string` | Accessible group name when `label` supplies none. `label` wins where both are given — it names the group from visible text, and emitting both would leave this string computed by nothing. |
| `defaultValue?` | `string` | — |
| `disabled?` | `boolean` | — |
| `label?` | `string` | Visible group label, rendered above the options and wired as the group's name. |
| `name?` | `string` | — |
| `onValueChange?` | `(value: string) => void` | — |
| `options` | `readonly RadioOption[]` | — |
| `orientation?` | `"horizontal" \| "vertical"` | Layout, and — when given — the arrow-key axis. Omitted, the options stack and BOTH axes move between them, which is what Radix does with no orientation and what every caller that never passed one already had. Pass a value only to pin the axis as well. |
| `value?` | `string` | — |
| `variant?` | `"list" \| "segmented"` | `list` is a stack of labelled radios; `segmented` is a compact segment strip. |

**Related:** [RadioGroupProps](/reference/studio-sdk/components#radiogroupprops)


## RadioGroupProps

```ts
interface RadioGroupProps
```

**Properties**

| Property | Type | Description |
|---|---|---|
| `aria-label?` | `string` | Accessible group name when `label` supplies none. `label` wins where both are given — it names the group from visible text, and emitting both would leave this string computed by nothing. |
| `defaultValue?` | `string` | — |
| `disabled?` | `boolean` | — |
| `label?` | `string` | Visible group label, rendered above the options and wired as the group's name. |
| `name?` | `string` | — |
| `onValueChange?` | `(value: string) => void` | — |
| `options` | `readonly RadioOption[]` | — |
| `orientation?` | `"horizontal" \| "vertical"` | Layout, and — when given — the arrow-key axis. Omitted, the options stack and BOTH axes move between them, which is what Radix does with no orientation and what every caller that never passed one already had. Pass a value only to pin the axis as well. |
| `value?` | `string` | — |
| `variant?` | `"list" \| "segmented"` | `list` is a stack of labelled radios; `segmented` is a compact segment strip. |

**Related:** [RadioOption](/reference/studio-sdk/components#radiooption)


## RadioOption

```ts
interface RadioOption
```

**Properties**

| Property | Type | Description |
|---|---|---|
| `disabled?` | `boolean` | — |
| `icon?` | `ReactNode` | Mark rendered alongside the label. Decorative — the label carries the name. |
| `label` | `string` | — |
| `value` | `string` | — |
| `visuallyHiddenLabel?` | `boolean` | Render the label for screen readers only; it stays the option's accessible name. |


## RevealInput

```ts
function RevealInput(props: RevealInputProps): Element
```

**Props**

| Prop | Type | Description |
|---|---|---|
| `aria-label?` | `string` | Accessible name for the inner input when no visual `label` is rendered. |
| `disabled?` | `boolean` | — |
| `idPrefix?` | `string` | — |
| `label?` | `string` | — |
| `onChange?` | `(value: string) => void` | — |
| `placeholder?` | `string` | — |
| `readOnly?` | `boolean` | — |
| `value` | `string` | — |

**Related:** [RevealInputProps](/reference/studio-sdk/components#revealinputprops)


## RevealInputProps

```ts
interface RevealInputProps
```

**Properties**

| Property | Type | Description |
|---|---|---|
| `aria-label?` | `string` | Accessible name for the inner input when no visual `label` is rendered. |
| `disabled?` | `boolean` | — |
| `idPrefix?` | `string` | — |
| `label?` | `string` | — |
| `onChange?` | `(value: string) => void` | — |
| `placeholder?` | `string` | — |
| `readOnly?` | `boolean` | — |
| `value` | `string` | — |


## SchedulingIcon

```ts
const SchedulingIcon: IconComponent
```

Scheduling: a calendar grid with its binding posts.

**Related:** [IconComponent](/reference/studio-sdk/components#iconcomponent)


## ScrollRegion

```ts
function ScrollRegion(props: ScrollRegionProps): Element
```

**Props**

| Prop | Type | Description |
|---|---|---|
| `children` | `ReactNode` | — |
| `className?` | `string` | — |
| `data-testid?` | `string` | The one arbitrary attribute this component forwards. The measured attributes — `tabindex`, `role` and `aria-label` — are the component's own and appear and disappear with the overflow, so a general attribute spread would let a caller set them statically and defeat the measurement. A test hook cannot, which is why it is the exception rather than the first of a set. |
| `label` | `string` | The region's accessible name, applied only while it actually scrolls. |
| `ref?` | `Ref<HTMLDivElement>` | A consumer ref for the scrolling `div` itself. |
| `style?` | `CSSProperties` | — |

**Related:** [ScrollRegionProps](/reference/studio-sdk/components#scrollregionprops)


## ScrollRegionProps

```ts
interface ScrollRegionProps
```

**Properties**

| Property | Type | Description |
|---|---|---|
| `children` | `ReactNode` | — |
| `className?` | `string` | — |
| `data-testid?` | `string` | The one arbitrary attribute this component forwards. The measured attributes — `tabindex`, `role` and `aria-label` — are the component's own and appear and disappear with the overflow, so a general attribute spread would let a caller set them statically and defeat the measurement. A test hook cannot, which is why it is the exception rather than the first of a set. |
| `label` | `string` | The region's accessible name, applied only while it actually scrolls. |
| `ref?` | `Ref<HTMLDivElement>` | A consumer ref for the scrolling `div` itself. |
| `style?` | `CSSProperties` | — |


## SearchIcon

```ts
const SearchIcon: IconComponent
```

A magnifier.

**Related:** [IconComponent](/reference/studio-sdk/components#iconcomponent)


## Select

```ts
function Select(props: SelectArgs): Element
```

**Parameters**

| Parameter | Type | Default | Description |
|---|---|---|---|
| `props` | `SelectArgs` | — | — |


## SelectGroup

```ts
interface SelectGroup
```

A labelled cluster of options (e.g. tools grouped by tag).

**Properties**

| Property | Type | Description |
|---|---|---|
| `label` | `string` | — |
| `options` | `readonly SelectOption[]` | — |

**Related:** [SelectOption](/reference/studio-sdk/components#selectoption)


## SelectGroupsProps

```ts
interface SelectGroupsProps extends SelectSharedProps
```

The GROUPED listbox: labelled clusters, each carrying its own options.

**Properties**

| Property | Type | Description |
|---|---|---|
| `aria-label?` | `string` | Accessible name for the trigger when it is not wired to an enclosing `Field`. A `Field` wins where both are given — it names the trigger from visible text, and `aria-label` would override that native label and leave the visible text out of the accessible name (WCAG 2.5.3). |
| `defaultValue?` | `string` | — |
| `disabled?` | `boolean` | — |
| `groups` | `readonly SelectGroup[]` | — |
| `name?` | `string` | — |
| `onValueChange?` | `(value: string) => void` | — |
| `placeholder?` | `string` | — |
| `value?` | `string` | — |

**Related:** [SelectGroup](/reference/studio-sdk/components#selectgroup)


## SelectOption

```ts
interface SelectOption
```

**Properties**

| Property | Type | Description |
|---|---|---|
| `disabled?` | `boolean` | — |
| `label` | `string` | — |
| `value` | `string` | — |


## SelectProps

```ts
interface SelectProps extends SelectSharedProps
```

The FLAT listbox: one ungrouped run of options.

**Properties**

| Property | Type | Description |
|---|---|---|
| `aria-label?` | `string` | Accessible name for the trigger when it is not wired to an enclosing `Field`. A `Field` wins where both are given — it names the trigger from visible text, and `aria-label` would override that native label and leave the visible text out of the accessible name (WCAG 2.5.3). |
| `defaultValue?` | `string` | — |
| `disabled?` | `boolean` | — |
| `name?` | `string` | — |
| `onValueChange?` | `(value: string) => void` | — |
| `options` | `readonly SelectOption[]` | — |
| `placeholder?` | `string` | — |
| `value?` | `string` | — |

**Related:** [SelectOption](/reference/studio-sdk/components#selectoption)


## ServedEndpointsIcon

```ts
const ServedEndpointsIcon: IconComponent
```

Served endpoints: a broadcast node radiating to the tools it serves.

**Related:** [IconComponent](/reference/studio-sdk/components#iconcomponent)


## SettingsIcon

```ts
const SettingsIcon: IconComponent
```

Settings: an eight-tooth cog around its bore.

**Related:** [IconComponent](/reference/studio-sdk/components#iconcomponent)


## SignOutIcon

```ts
const SignOutIcon: IconComponent
```

Sign out: a door with an outbound arrow.

**Related:** [IconComponent](/reference/studio-sdk/components#iconcomponent)


## Skeleton

```ts
function Skeleton(props: SkeletonProps): Element
```

**Props**

| Prop | Type | Description |
|---|---|---|
| `className?` | `string` | — |
| `height?` | `string \| number` | — |
| `style?` | `CSSProperties` | — |
| `width?` | `string \| number` | — |

**Related:** [SkeletonProps](/reference/studio-sdk/components#skeletonprops)


## SkeletonProps

```ts
interface SkeletonProps extends SurfaceProps
```

**Properties**

| Property | Type | Description |
|---|---|---|
| `className?` | `string` | — |
| `height?` | `string \| number` | — |
| `style?` | `CSSProperties` | — |
| `width?` | `string \| number` | — |


## SortAscIcon

```ts
const SortAscIcon: IconComponent
```

Ascending sort: an up arrow beside narrow-to-wide rows.

**Related:** [IconComponent](/reference/studio-sdk/components#iconcomponent)


## SortDescIcon

```ts
const SortDescIcon: IconComponent
```

Descending sort: a down arrow beside wide-to-narrow rows.

**Related:** [IconComponent](/reference/studio-sdk/components#iconcomponent)


## Spinner

```ts
function Spinner(props: SpinnerProps): Element
```

**Props**

| Prop | Type | Description |
|---|---|---|
| `className?` | `string` | — |
| `label?` | `string` | — |
| `style?` | `CSSProperties` | — |

**Related:** [SpinnerProps](/reference/studio-sdk/components#spinnerprops)


## SpinnerProps

```ts
interface SpinnerProps extends SurfaceProps
```

**Properties**

| Property | Type | Description |
|---|---|---|
| `className?` | `string` | — |
| `label?` | `string` | — |
| `style?` | `CSSProperties` | — |


## Stack

```ts
function Stack(props: StackProps): ReactElement
```

A vertical flex column on the spacing scale.

**Props**

| Prop | Type | Description |
|---|---|---|
| `children` | `ReactNode` | — |
| `className?` | `string` | — |
| `gap?` | `2 \| 3 \| 4 \| 6` | The gap step; 4 is the default and needs no modifier class. |
| `ref?` | `Ref<HTMLDivElement>` | A consumer ref for the wrapper element itself. |
| `style?` | `CSSProperties` | — |

**Related:** [StackProps](/reference/studio-sdk/components#stackprops)


## StackProps

```ts
interface StackProps extends LayoutElementProps
```

**Properties**

| Property | Type | Description |
|---|---|---|
| `children` | `ReactNode` | — |
| `className?` | `string` | — |
| `gap?` | `2 \| 3 \| 4 \| 6` | The gap step; 4 is the default and needs no modifier class. |
| `ref?` | `Ref<HTMLDivElement>` | A consumer ref for the wrapper element itself. |
| `style?` | `CSSProperties` | — |


## StorageIcon

```ts
const StorageIcon: IconComponent
```

Storage: a database cylinder with two shelves.

**Related:** [IconComponent](/reference/studio-sdk/components#iconcomponent)


## SunIcon

```ts
const SunIcon: IconComponent
```

Light theme.

**Related:** [IconComponent](/reference/studio-sdk/components#iconcomponent)


## SystemIcon

```ts
const SystemIcon: IconComponent
```

System: a processor die with its pins.

**Related:** [IconComponent](/reference/studio-sdk/components#iconcomponent)


## TBody

```ts
function TBody(props: TableSectionProps): Element
```

**Props**

| Prop | Type | Description |
|---|---|---|
| `children?` | `ReactNode` | — |
| `ref?` | `Ref<HTMLTableSectionElement>` | A consumer ref for the section element itself. |

**Related:** [TableSectionProps](/reference/studio-sdk/components#tablesectionprops)


## TD

```ts
function TD(props: TDProps): Element
```

**Props**

| Prop | Type | Description |
|---|---|---|
| `children?` | `ReactNode` | — |
| `numeric?` | `boolean` | — |
| `ref?` | `Ref<HTMLTableCellElement>` | A consumer ref for the `<td>` itself. |

**Related:** [TDProps](/reference/studio-sdk/components#tdprops)


## TDProps

```ts
interface TDProps extends TdHTMLAttributes<HTMLTableCellElement>, NumericColumnProps
```

Right-align a column of digits on tabular figures.

**Properties**

| Property | Type | Description |
|---|---|---|
| `children?` | `ReactNode` | — |
| `numeric?` | `boolean` | — |
| `ref?` | `Ref<HTMLTableCellElement>` | A consumer ref for the `<td>` itself. |

**Related:** [NumericColumnProps](/reference/studio-sdk/components#numericcolumnprops)


## TH

```ts
function TH(props: THProps): Element
```

**Props**

| Prop | Type | Description |
|---|---|---|
| `children?` | `ReactNode` | — |
| `numeric?` | `boolean` | — |
| `ref?` | `Ref<HTMLTableCellElement>` | A consumer ref for the `<th>` itself. |

**Related:** [THProps](/reference/studio-sdk/components#thprops)


## THProps

```ts
interface THProps extends ThHTMLAttributes<HTMLTableCellElement>, NumericColumnProps
```

A header cell. `scope` defaults to `col` because that is what a `<th>` in the
header row IS, and without it nothing associates the cell with the column
below it: a screen reader announcing a data cell has no header to name it.
A row header — the leading cell of a body row — passes `scope="row"`.

**Properties**

| Property | Type | Description |
|---|---|---|
| `children?` | `ReactNode` | — |
| `numeric?` | `boolean` | — |
| `ref?` | `Ref<HTMLTableCellElement>` | A consumer ref for the `<th>` itself. |

**Related:** [NumericColumnProps](/reference/studio-sdk/components#numericcolumnprops)


## THead

```ts
function THead(props: TableSectionProps): Element
```

**Props**

| Prop | Type | Description |
|---|---|---|
| `children?` | `ReactNode` | — |
| `ref?` | `Ref<HTMLTableSectionElement>` | A consumer ref for the section element itself. |

**Related:** [TableSectionProps](/reference/studio-sdk/components#tablesectionprops)


## TOKEN_NAMES

```ts
const TOKEN_NAMES: readonly ["--tai-color-bg", "--tai-color-surface", "--tai-color-surface-raised", "--tai-color-surface-disabled", "--tai-color-code-bg", "--tai-color-border", "--tai-color-border-strong", "--tai-color-control-border", "--tai-color-border-disabled", "--tai-color-text", "--tai-color-heading", "--tai-color-text-muted", "--tai-color-text-disabled", "--tai-color-placeholder", "--tai-color-decor", "--tai-color-accent", "--tai-color-accent-hover", "--tai-color-accent-on-tint", "--tai-color-on-accent", "--tai-color-accent-tint", "--tai-color-ok-text", "--tai-color-err-text", "--tai-color-warn-text", "--tai-color-ok-fill", "--tai-color-err-fill", "--tai-color-warn-fill", "--tai-color-ok-tint", "--tai-color-err-tint", "--tai-color-warn-tint", "--tai-color-on-fill", "--tai-color-focus-ring", "--tai-color-scrim", "--tai-color-prose-link", "--tai-color-prose-link-hover", "--tai-color-syntax-key", "--tai-color-syntax-string", "--tai-color-syntax-number", "--tai-color-syntax-bool", "--tai-color-primary", "--tai-color-primary-text", "--tai-color-danger", "--tai-color-danger-text", "--tai-color-danger-hover", "--tai-color-danger-surface", "--tai-color-success", "--tai-color-warning", "--tai-space-1", "--tai-space-2", "--tai-space-3", "--tai-space-4", "--tai-space-5", "--tai-space-6", "--tai-space-8", "--tai-radius-sm", "--tai-radius-code", "--tai-radius-md", "--tai-radius-tile", "--tai-radius-lg", "--tai-radius-overlay", "--tai-radius-full", "--tai-font-sans", "--tai-font-mono", "--tai-text-display", "--tai-text-xl", "--tai-text-section", "--tai-text-lg", "--tai-text-md", "--tai-text-sm", "--tai-text-code", "--tai-text-xs", "--tai-control-height", "--tai-control-height-coarse", "--tai-motion-fast", "--tai-motion-base", "--tai-shadow-lift-color", "--tai-shadow-overlay-color", "--tai-shadow-lift", "--tai-shadow-overlay", "--tai-shadow-sm", "--tai-shadow-md", "--tai-z-sticky", "--tai-z-dropdown", "--tai-z-overlay", "--tai-z-dialog", "--tai-z-popover", "--tai-z-tooltip"]
```

The documented CSS-variable token names — the Studio-plugin styling API. A
snapshot test pins this list so the contract can't drift silently. The
SDK compatibility version bumps if any of these are renamed/removed; the list
grows additively.

The `--tai-dark-*` half of each themed pair is deliberately NOT here. Those
names are the theming mechanism's own storage — the two blocks in `tokens.css`
that apply the dark theme are the only readers, and the scanner test holds
them to that. A plugin styles against the name WITHOUT the `dark` segment and
gets whichever half the viewer's theme resolves to.


## TR

```ts
function TR(props: TableRowProps): Element
```

**Props**

| Prop | Type | Description |
|---|---|---|
| `children?` | `ReactNode` | — |
| `ref?` | `Ref<HTMLTableRowElement>` | A consumer ref for the `<tr>` itself. |

**Related:** [TableRowProps](/reference/studio-sdk/components#tablerowprops)


## TabItem

```ts
interface TabItem
```

**Properties**

| Property | Type | Description |
|---|---|---|
| `content` | `ReactNode` | — |
| `disabled?` | `boolean` | — |
| `label` | `string` | — |
| `value` | `string` | — |


## Table

```ts
function Table(props: TableProps): Element
```

**Props**

| Prop | Type | Description |
|---|---|---|
| `children?` | `ReactNode` | — |
| `ref?` | `Ref<HTMLTableElement>` | A consumer ref for the `<table>` itself. |

**Related:** [TableProps](/reference/studio-sdk/components#tableprops)


## TableProps

```ts
interface TableProps extends HTMLAttributes<HTMLTableElement>
```

**Properties**

| Property | Type | Description |
|---|---|---|
| `children?` | `ReactNode` | — |
| `ref?` | `Ref<HTMLTableElement>` | A consumer ref for the `<table>` itself. |


## TableRowProps

```ts
interface TableRowProps extends HTMLAttributes<HTMLTableRowElement>
```

**Properties**

| Property | Type | Description |
|---|---|---|
| `children?` | `ReactNode` | — |
| `ref?` | `Ref<HTMLTableRowElement>` | A consumer ref for the `<tr>` itself. |


## TableSectionProps

```ts
interface TableSectionProps extends HTMLAttributes<HTMLTableSectionElement>
```

`<thead>` and `<tbody>` take the same surface: the native one.

**Properties**

| Property | Type | Description |
|---|---|---|
| `children?` | `ReactNode` | — |
| `ref?` | `Ref<HTMLTableSectionElement>` | A consumer ref for the section element itself. |


## Tabs

```ts
function Tabs(props: TabsProps): Element
```

**Props**

| Prop | Type | Description |
|---|---|---|
| `activationMode?` | `"manual" \| "automatic"` | `'manual'` commits on Enter/Space/click only — arrows just move focus. |
| `defaultValue?` | `string` | — |
| `items` | `readonly TabItem[]` | — |
| `onValueChange?` | `(value: string) => void` | — |
| `value?` | `string` | — |

**Related:** [TabsProps](/reference/studio-sdk/components#tabsprops)


## TabsProps

```ts
interface TabsProps
```

**Properties**

| Property | Type | Description |
|---|---|---|
| `activationMode?` | `"manual" \| "automatic"` | `'manual'` commits on Enter/Space/click only — arrows just move focus. |
| `defaultValue?` | `string` | — |
| `items` | `readonly TabItem[]` | — |
| `onValueChange?` | `(value: string) => void` | — |
| `value?` | `string` | — |

**Related:** [TabItem](/reference/studio-sdk/components#tabitem)


## TagChips

```ts
function TagChips(props: TagChipsProps): ReactNode
```

Read-only chips for a list of tags. Renders nothing when there are none.

**Props**

| Prop | Type | Description |
|---|---|---|
| `tags` | `readonly string[]` | — |

**Related:** [TagChipsProps](/reference/studio-sdk/components#tagchipsprops)


## TagChipsProps

```ts
interface TagChipsProps
```

**Properties**

| Property | Type | Description |
|---|---|---|
| `tags` | `readonly string[]` | — |


## TagVocabularyEntry

```ts
interface TagVocabularyEntry
```

**Properties**

| Property | Type | Description |
|---|---|---|
| `count` | `number` | — |
| `label` | `string` | — |
| `token` | `string` | — |


## TagsInput

```ts
function TagsInput(props: TagsInputProps): ReactNode
```

Controlled tag editor. The caller owns the `value` list; `onChange` receives the
next list on every add/remove. A blank or duplicate entry is ignored (never a
silent duplicate chip). Enter or comma commits the draft chip.

The draft input is a NATIVE-ATTRIBUTE PASS-THROUGH (the family `inputs.tsx`
documents): inside a `Field` it renders the SDK's `TextInput`, which spreads
`useFieldControl()` and so claims the Field's control id, taking the Field's
visible label as its name; mounted OUTSIDE a `Field` it is named by a caller
`aria-label`, which the editor forwards to that same input. Without one a bare
editor is unnamed — the published component owns the naming route rather than
assuming a `Field` is always present. When a `Field` DOES name it, a caller
`aria-label` still has to CONTAIN that visible label (WCAG 2.5.3, Label in
Name); `components/field-group.test.ts` enforces that at every call site.

**Props**

| Prop | Type | Description |
|---|---|---|
| `aria-label?` | `string` | The draft input's accessible name when no enclosing `Field` supplies one. Forwarded to the input as a native attribute — see the docblock below. |
| `disabled?` | `boolean` | — |
| `itemNoun?` | `string` | The noun for the placeholder + the Add/Remove control names ("tag" by default). Set it (e.g. "badge") so a second editor on the same surface reads and, above all, has DISTINCT accessible control names — two "Add tag" buttons would be ambiguous to a name-based query and a screen reader alike. |
| `onChange` | `(next: string[]) => void` | — |
| `value` | `readonly string[]` | — |

**Related:** [TagsInputProps](/reference/studio-sdk/components#tagsinputprops)


## TagsInputProps

```ts
interface TagsInputProps
```

**Properties**

| Property | Type | Description |
|---|---|---|
| `aria-label?` | `string` | The draft input's accessible name when no enclosing `Field` supplies one. Forwarded to the input as a native attribute — see the docblock below. |
| `disabled?` | `boolean` | — |
| `itemNoun?` | `string` | The noun for the placeholder + the Add/Remove control names ("tag" by default). Set it (e.g. "badge") so a second editor on the same surface reads and, above all, has DISTINCT accessible control names — two "Add tag" buttons would be ambiguous to a name-based query and a screen reader alike. |
| `onChange` | `(next: string[]) => void` | — |
| `value` | `readonly string[]` | — |


## TemplatesIcon

```ts
const TemplatesIcon: IconComponent
```

Templates: a stack of two sheets, the front one offset.

**Related:** [IconComponent](/reference/studio-sdk/components#iconcomponent)


## TextInput

```ts
function TextInput(props: TextInputProps): Element
```

**Parameters**

| Parameter | Type | Default | Description |
|---|---|---|---|
| `props` | `TextInputProps` | — | — |

**Related:** [TextInputProps](/reference/studio-sdk/components#textinputprops)


## TextInputProps

```ts
type TextInputProps = InputHTMLAttributes<HTMLInputElement> & ControlRefProps<HTMLInputElement>
```


## Textarea

```ts
function Textarea(props: TextareaProps): Element
```

**Parameters**

| Parameter | Type | Default | Description |
|---|---|---|---|
| `props` | `TextareaProps` | — | — |

**Related:** [TextareaProps](/reference/studio-sdk/components#textareaprops)


## TextareaProps

```ts
type TextareaProps = TextareaHTMLAttributes<HTMLTextAreaElement> & ControlRefProps<HTMLTextAreaElement>
```


## TokenName

```ts
type TokenName = typeof TOKEN_NAMES[number]
```

**Related:** [TOKEN_NAMES](/reference/studio-sdk/components#token_names)


## ToolPicker

```ts
function ToolPicker(props: ToolPickerProps): Element
```

**Props**

| Prop | Type | Description |
|---|---|---|
| `agentToolNames?` | `ReadonlySet<string>` | Tool names that are agent run tools; their option labels are suffixed " (agent)". |
| `aria-label?` | `string` | Accessible name for the tool listbox when `label` renders none and no enclosing `Field` names it. `label` wins where both are given: it names the control from visible text, and the `Select` drops a caller name it would otherwise override. |
| `badgesByTool?` | `Readonly<Record<string, readonly string[]>>` | Per-tool DECLARED capability badges (native ∪ overlay). When present, the selected tool's badges render as read-only chips beneath the select — purely informational, never affecting which options show or how they group. |
| `disabled?` | `boolean` | — |
| `displayNames?` | `Readonly<Record<string, string>>` | Human display names keyed by raw tool name (from the tool-meta overlay). A mapped option's label reads `Display Name (raw_name)`; an unmapped name — or a mapping equal to the raw name — reads the bare raw name. The value `onChange` emits stays the raw name in every case. |
| `excludeNames?` | `readonly string[]` | Names removed from the options (the server remains the authority). |
| `idPrefix?` | `string` | — |
| `label?` | `string` | — |
| `onChange` | `(toolName: string) => void` | — |
| `placeholder?` | `string` | — |
| `tagsByTool?` | `Readonly<Record<string, readonly string[]>>` | Per-tool native tags; when present, options are grouped and a tag filter shows. |
| `toolNames` | `readonly string[]` | — |
| `value` | `string \| null` | — |

**Related:** [ToolPickerProps](/reference/studio-sdk/components#toolpickerprops)


## ToolPickerProps

```ts
interface ToolPickerProps
```

**Properties**

| Property | Type | Description |
|---|---|---|
| `agentToolNames?` | `ReadonlySet<string>` | Tool names that are agent run tools; their option labels are suffixed " (agent)". |
| `aria-label?` | `string` | Accessible name for the tool listbox when `label` renders none and no enclosing `Field` names it. `label` wins where both are given: it names the control from visible text, and the `Select` drops a caller name it would otherwise override. |
| `badgesByTool?` | `Readonly<Record<string, readonly string[]>>` | Per-tool DECLARED capability badges (native ∪ overlay). When present, the selected tool's badges render as read-only chips beneath the select — purely informational, never affecting which options show or how they group. |
| `disabled?` | `boolean` | — |
| `displayNames?` | `Readonly<Record<string, string>>` | Human display names keyed by raw tool name (from the tool-meta overlay). A mapped option's label reads `Display Name (raw_name)`; an unmapped name — or a mapping equal to the raw name — reads the bare raw name. The value `onChange` emits stays the raw name in every case. |
| `excludeNames?` | `readonly string[]` | Names removed from the options (the server remains the authority). |
| `idPrefix?` | `string` | — |
| `label?` | `string` | — |
| `onChange` | `(toolName: string) => void` | — |
| `placeholder?` | `string` | — |
| `tagsByTool?` | `Readonly<Record<string, readonly string[]>>` | Per-tool native tags; when present, options are grouped and a tag filter shows. |
| `toolNames` | `readonly string[]` | — |
| `value` | `string \| null` | — |


## ToolsIcon

```ts
const ToolsIcon: IconComponent
```

Tools: an open-ended spanner over its handle.

**Related:** [IconComponent](/reference/studio-sdk/components#iconcomponent)


## Tooltip

```ts
function Tooltip(props: TooltipProps): Element
```

**Props**

| Prop | Type | Description |
|---|---|---|
| `children` | `ReactElement` | The element the bubble describes and hangs off.  It is a single ELEMENT, not any node: Radix clones its props onto it, so a string throws. The type admits a FRAGMENT — `<></>` is a `ReactElement` — which would render children carrying none of the hover/focus wiring nor the `aria-describedby` back to the bubble, so assertSlotElement rejects that one at runtime. |
| `content` | `ReactNode` | — |
| `delayDuration?` | `number` | — |

**Related:** [TooltipProps](/reference/studio-sdk/components#tooltipprops)


## TooltipProps

```ts
interface TooltipProps
```

**Properties**

| Property | Type | Description |
|---|---|---|
| `children` | `ReactElement` | The element the bubble describes and hangs off.  It is a single ELEMENT, not any node: Radix clones its props onto it, so a string throws. The type admits a FRAGMENT — `<></>` is a `ReactElement` — which would render children carrying none of the hover/focus wiring nor the `aria-describedby` back to the bubble, so assertSlotElement rejects that one at runtime. |
| `content` | `ReactNode` | — |
| `delayDuration?` | `number` | — |


## UNTAGGED_TOKEN

```ts
const UNTAGGED_TOKEN: "__untagged__"
```

Reserved filter token for items carrying NO tag; namespaced so it never
 collides with a real tag.


## VersionHistoryEntry

```ts
interface VersionHistoryEntry
```

One version row — the kind-agnostic shape every consumer projects into.

**Properties**

| Property | Type | Description |
|---|---|---|
| `body` | `unknown` | — |
| `created_at` | `string` | — |
| `is_current` | `boolean` | — |
| `tags?` | `readonly string[]` | The version's generic label list, surfaced as a Tags column when present. |
| `version` | `number` | — |


## VersionHistoryPanel

```ts
function VersionHistoryPanel(props: VersionHistoryPanelProps): ReactNode
```

**Props**

| Prop | Type | Description |
|---|---|---|
| `onEditTags?` | `(version: number, tags: string[]) => Promise<void>` | Replace one version's tag annotation. When provided, a per-row "Edit tags" action opens a dialog seeded from the row's tags and SAVE calls this with the NEW list; the panel awaits the returned promise with internal pending/error state. Omitted ⇒ no edit affordance (tags stay read-only). |
| `onRollback` | `(version: number) => void` | Roll the active pointer to `version`. The consumer owns the mutation. |
| `onView?` | `(version: number) => void` | Fired when a version is selected for viewing (optional). |
| `readOnly?` | `boolean` | A read-only history view: the version list and each version's body stay visible (history is a read surface), but EVERY mutating action is hidden — the per-row Rollback and, where `onEditTags` is supplied, Edit tags. Defaults to interactive. |
| `rollbackConfirmDescription?` | `string` | Extra consequence copy appended to the rollback confirm dialog's description (announced to assistive tech via the dialog's `aria-describedby`). Consumers whose rollback has an immediate live effect (e.g. re-pointing access-control enforcement) pass a sentence stating so; omitted when there is nothing extra. |
| `rollbackError?` | `string` | A loud rollback failure message to surface in the confirm dialog. |
| `rollbackPending?` | `boolean` | The consumer's rollback mutation is in flight. |
| `versions` | `readonly VersionHistoryEntry[]` | — |

**Related:** [VersionHistoryPanelProps](/reference/studio-sdk/components#versionhistorypanelprops)


## VersionHistoryPanelProps

```ts
interface VersionHistoryPanelProps
```

**Properties**

| Property | Type | Description |
|---|---|---|
| `onEditTags?` | `(version: number, tags: string[]) => Promise<void>` | Replace one version's tag annotation. When provided, a per-row "Edit tags" action opens a dialog seeded from the row's tags and SAVE calls this with the NEW list; the panel awaits the returned promise with internal pending/error state. Omitted ⇒ no edit affordance (tags stay read-only). |
| `onRollback` | `(version: number) => void` | Roll the active pointer to `version`. The consumer owns the mutation. |
| `onView?` | `(version: number) => void` | Fired when a version is selected for viewing (optional). |
| `readOnly?` | `boolean` | A read-only history view: the version list and each version's body stay visible (history is a read surface), but EVERY mutating action is hidden — the per-row Rollback and, where `onEditTags` is supplied, Edit tags. Defaults to interactive. |
| `rollbackConfirmDescription?` | `string` | Extra consequence copy appended to the rollback confirm dialog's description (announced to assistive tech via the dialog's `aria-describedby`). Consumers whose rollback has an immediate live effect (e.g. re-pointing access-control enforcement) pass a sentence stating so; omitted when there is nothing extra. |
| `rollbackError?` | `string` | A loud rollback failure message to surface in the confirm dialog. |
| `rollbackPending?` | `boolean` | The consumer's rollback mutation is in flight. |
| `versions` | `readonly VersionHistoryEntry[]` | — |

**Related:** [VersionHistoryEntry](/reference/studio-sdk/components#versionhistoryentry)


## ViewMode

```ts
type ViewMode = "list" | "cards"
```

The two entity-screen layouts.


## ViewToggle

```ts
function ViewToggle(props: ViewToggleProps): ReactNode
```

The icon-only segmented switch. Controlled: pair it with `useViewMode`.

**Props**

| Prop | Type | Description |
|---|---|---|
| `aria-label` | `string` | The control's accessible group name (e.g. "Tools view"). |
| `disabled?` | `boolean` | — |
| `onValueChange` | `(mode: ViewMode) => void` | — |
| `value` | `ViewMode` | — |

**Related:** [ViewToggleProps](/reference/studio-sdk/components#viewtoggleprops)


## ViewToggleProps

```ts
interface ViewToggleProps
```

**Properties**

| Property | Type | Description |
|---|---|---|
| `aria-label` | `string` | The control's accessible group name (e.g. "Tools view"). |
| `disabled?` | `boolean` | — |
| `onValueChange` | `(mode: ViewMode) => void` | — |
| `value` | `ViewMode` | — |

**Related:** [ViewMode](/reference/studio-sdk/components#viewmode)


## XCircleIcon

```ts
const XCircleIcon: IconComponent
```

Failure.

**Related:** [IconComponent](/reference/studio-sdk/components#iconcomponent)


## baseNameOf

```ts
function baseNameOf(name: string): string
```

The base tool name a variant belongs to: the substring before the first
underscore, or the whole name when it has none. `tool`, `tool_a`, `tool_a_b`
all resolve to `tool`; `x_chain` and `x_batch` both resolve to `x`.

**Parameters**

| Parameter | Type | Default | Description |
|---|---|---|---|
| `name` | `string` | — | — |


## buildTagVocabulary

```ts
function buildTagVocabulary<T>(
  items: readonly T[],
  getTags: (item: T) => readonly string[],
  selectedTags: readonly string[],
  untaggedLabel: string,
): TagVocabularyEntry[]
```

The tag vocabulary over `items`, name-sorted, plus an untagged pseudo-tag
 (labelled `untaggedLabel`) when any item is untagged; a selected token whose
 items vanished still appears (count 0) so it can be cleared.

**Parameters**

| Parameter | Type | Default | Description |
|---|---|---|---|
| `items` | `readonly T[]` | — | — |
| `getTags` | `(item: T) => readonly string[]` | — | — |
| `selectedTags` | `readonly string[]` | — | — |
| `untaggedLabel` | `string` | — | — |

**Related:** [TagVocabularyEntry](/reference/studio-sdk/components#tagvocabularyentry)


## childFolders

```ts
function childFolders(folders: readonly Folder[], parentId: string | null): Folder[]
```

The subfolders directly under `parentId` (`null` = the root directory), name-sorted.

**Parameters**

| Parameter | Type | Default | Description |
|---|---|---|---|
| `folders` | `readonly Folder[]` | — | — |
| `parentId` | `string \| null` | — | — |

**Related:** [Folder](/reference/studio-sdk/components#folder)


## diffJson

```ts
function diffJson(before: unknown, after: unknown): JsonDiffRow[]
```

The pure structural diff — every leaf difference between `before` and `after`.

**Parameters**

| Parameter | Type | Default | Description |
|---|---|---|---|
| `before` | `unknown` | — | — |
| `after` | `unknown` | — | — |

**Related:** [JsonDiffRow](/reference/studio-sdk/components#jsondiffrow)


## folderPathTo

```ts
function folderPathTo(folders: readonly Folder[], folderId: string | null): Folder[]
```

The ordered ancestor path from the root down to (and including) `folderId`, for a
breadcrumb. `null` is the root (an empty path). The walk is bounded by the folder
count, so a cyclic or dangling chain truncates instead of looping.

**Parameters**

| Parameter | Type | Default | Description |
|---|---|---|---|
| `folders` | `readonly Folder[]` | — | — |
| `folderId` | `string \| null` | — | — |

**Related:** [Folder](/reference/studio-sdk/components#folder)


## formatRangeLabel

```ts
function formatRangeLabel(
  value: DateRangeValue,
  presets: readonly DateRangePreset[],
): string
```

**Parameters**

| Parameter | Type | Default | Description |
|---|---|---|---|
| `value` | `DateRangeValue` | — | — |
| `presets` | `readonly DateRangePreset[]` | `DEFAULT_DATE_RANGE_PRESETS` | — |

**Related:** [DateRangePreset](/reference/studio-sdk/components#daterangepreset), [DateRangeValue](/reference/studio-sdk/components#daterangevalue)


## groupByKind

```ts
function groupByKind(
  extensions: readonly ({ kind: string; name: string })[],
): ExtensionKindGroup[]
```

Group a flat extension list by `kind`, for the picker. Groups are ordered by
kind and each group's members by name, so the render order is stable regardless
of the server's list order. The `backend` group is flagged non-stackable.

**Parameters**

| Parameter | Type | Default | Description |
|---|---|---|---|
| `extensions` | `readonly ({ kind: string; name: string })[]` | — | — |

**Related:** [ExtensionKindGroup](/reference/studio-sdk/components#extensionkindgroup)


## groupIntoFamilies

```ts
function groupIntoFamilies(
  extensions: readonly ({ kind: string; name: string })[],
): ExtensionFamily[]
```

Group a flat extension list into families keyed by base tool name. Families
are ordered by base name and each family's members by full name, so the render
order is stable regardless of the server's list order.

**Parameters**

| Parameter | Type | Default | Description |
|---|---|---|---|
| `extensions` | `readonly ({ kind: string; name: string })[]` | — | — |

**Related:** [ExtensionFamily](/reference/studio-sdk/components#extensionfamily)


## isSafeHttpUrl

```ts
function isSafeHttpUrl(url: string): boolean
```

True only for an absolute `http://`/`https://` URL. Everything else is unsafe.

**Parameters**

| Parameter | Type | Default | Description |
|---|---|---|---|
| `url` | `string` | — | — |


## kindVariant

```ts
function kindVariant(kind: string): string
```

Map a serialized extension `kind` to a Badge variant. Known skeleton kinds get
a themed color; any unrecognized kind falls back to the neutral variant (the
Badge itself also neutral-fallbacks, but choosing here keeps the mapping
explicit and testable).

**Parameters**

| Parameter | Type | Default | Description |
|---|---|---|---|
| `kind` | `string` | — | — |


## matchesSelectedTags

```ts
function matchesSelectedTags(
  itemTags: readonly string[],
  selected: ReadonlySet<string>,
): boolean
```

OR semantics: an empty selection matches everything; otherwise an item
 matches when it carries any selected tag, or is untagged and UNTAGGED_TOKEN
 is selected.

**Parameters**

| Parameter | Type | Default | Description |
|---|---|---|---|
| `itemTags` | `readonly string[]` | — | — |
| `selected` | `ReadonlySet<string>` | — | — |


## normalizeCustomRange

```ts
function normalizeCustomRange(
  fromInput: string,
  toInput: string,
  now: Date,
): { from: string; to: string }
```

A raw from/to pair resolved to a clean absolute range. Order matters: SWAP the
reversed pair first (earlier instant becomes start), THEN cap both ends at `now` —
capping first can leave a future instant on the post-swap end. The end is
INCLUSIVE of its minute, extended to `:59.999`, so `23:59` reaches the day's end.

**Parameters**

| Parameter | Type | Default | Description |
|---|---|---|---|
| `fromInput` | `string` | — | — |
| `toInput` | `string` | — | — |
| `now` | `Date` | — | — |


## openTargetProps

```ts
function openTargetProps(props: OpenTargetOptions): OpenTargetProps
```

Build the whole-target open props for one entry. See the module doc for the
contract; pass `onOpen: undefined` to get an inert entry (empty props).

**Props**

| Prop | Type | Description |
|---|---|---|
| `ignoreWithin?` | `string` | Extra selector(s) that also count as "nested interactive" for THIS surface,  merged with the built-in set. Use it for a marked non-`button`/`a` subtree  that owns its own clicks (e.g. an expandable preview keyed by a data  attribute). Standard CSS selector syntax, comma-separated. |
| `keyboard?` | `boolean` | Make the entry itself keyboard-operable: a `tabIndex: 0` focus stop with  Enter/Space activation. Leave off when a nested interactive element already  carries the accessible path (see the keyboard note above) — adding it there  would create a duplicate tab stop. |
| `onOpen` | `() => void \| undefined` | Open the entry. `undefined` disables the affordance entirely — the helper  returns empty props so the element stays inert (no cursor, no handlers),  which lets a caller wire the whole thing behind an optional prop. |

**Related:** [OpenTargetOptions](/reference/studio-sdk/components#opentargetoptions), [OpenTargetProps](/reference/studio-sdk/components#opentargetprops)


## overlayDetailsPatch

```ts
function overlayDetailsPatch(
  value: OverlayDetails,
): { display_name: string | null; tags: string[] }
```

The merge-patch a display-name + tags write sends: ONLY these two fields.

**Props**

| Prop | Type | Description |
|---|---|---|
| `displayName` | `string` | The raw display-name draft (unblank-normalized only at write time). |
| `tags` | `readonly string[]` | — |

**Related:** [OverlayDetails](/reference/studio-sdk/components#overlaydetails)


## parseMarkdown

```ts
function parseMarkdown(markdown: string): MarkdownBlock[]
```

Folds markdown source into an ordered list of blocks. Pure and total: any
string parses, and text that matches no block construct becomes a paragraph
rather than being discarded.

**Parameters**

| Parameter | Type | Default | Description |
|---|---|---|---|
| `markdown` | `string` | — | — |

**Related:** [MarkdownBlock](/reference/studio-sdk/components#markdownblock)


## safeHttpUrl

```ts
function safeHttpUrl(href: string): string | undefined
```

The absolute http(s) URL `href` denotes, or `undefined` when it denotes none.

The `//` is required rather than inferred from a successful `new URL()`,
because for a scheme that matches the document's own, the authority-less
spelling is a PATH: an anchor with `href="https:/settings"` on an https page
navigates to `/settings` on the CURRENT origin, while `new URL()` with no base
reads the same string as the host `settings`. Only the `//` form means the same
thing to both.

An absolute http(s) URL carrying `user:pass@` userinfo is rejected too: `.href`
re-serialization preserves the userinfo (unlike host tricks), so
`https://trusted.com@evil.com` would render as trusted.com yet navigate to
evil.com.

Callers render the returned string rather than their input, so the URL that was
judged is always the URL that is navigated.

**Parameters**

| Parameter | Type | Default | Description |
|---|---|---|---|
| `href` | `string` | — | — |


## useFieldControl

```ts
function useFieldControl(): FieldControlProps
```

Read the enclosing Field's wiring.

`id` and `aria-describedby` are `undefined` both when there is no Field AND
inside a group `Field`: a group has no single element to carry them, and the
group container itself already carries the name and the description. Only
`aria-invalid` still crosses that boundary, because `role="group"` does not
support it (ARIA 1.2), so the control is the sole place an errored group can
expose its invalid state.

**Related:** [FieldControlProps](/reference/studio-sdk/components#fieldcontrolprops)


## useFillViewport

```ts
function useFillViewport(): void
```

Opt the current page into full-height fill mode for as long as it is mounted.
The shell fills `<main>` to the viewport and lets the page's own root flex or
`height: 100%` down the chain. Must be called under a `PageFillProvider`
(the shell mounts one); outside it there is no wrapper to modify, so it throws
rather than silently do nothing.


## useOverflowRegion

```ts
function useOverflowRegion(
  ref: Ref<HTMLElement> | undefined,
  label: string,
): OverflowRegionAttributes
```

The conditional region attributes for a scrolling box, alongside the ref that
names it. The ref must go on the scrolling box ITSELF — a `<pre>`, a JSON
pane, or the `div` `ScrollRegion` renders. Use the hook when wrapping the
content in a `ScrollRegion` would give the surface a second scroller.

The box is measured as it is attached, whenever it or any child resizes, and
whenever its content changes — replaced, appended, or edited in place. A
replaced child is a NEW element, so the registration is re-pointed at the
current children before each measurement, adding and dropping only the ones
that actually changed; both sides of that are registrations on the shared
observers, not observers of this mount's own. The only DOM change a
measurement can cause is the region ATTRIBUTES this hook returns, and
attributes are deliberately left unobserved — that, not an absence of
mutation, is what stops the pair from re-triggering each other.

**Parameters**

| Parameter | Type | Default | Description |
|---|---|---|---|
| `ref` | `Ref<HTMLElement> \| undefined` | — | a consumer ref that wants the same element, or `undefined`. |
| `label` | `string` | — | its accessible name, applied only while it actually scrolls. |

**Related:** [OverflowRegionAttributes](/reference/studio-sdk/components#overflowregionattributes)


## usePageFillActive

```ts
function usePageFillActive(): boolean
```

Whether any mounted page currently asks for fill mode — the shell's cue to add
the fill modifiers to `<main>` and the page wrapper.


## useProseScrollRegions

```ts
function useProseScrollRegions(labels?: ProseScrollLabels): RefCallback<HTMLElement>
```

Instruments the scrollable surfaces under the element the returned ref is
attached to — every `<table>` and every `<pre>` — as scroll regions. React
cannot wrap markup it did not create, so this walks the DOM instead. A table
is moved into a `div.tai-scroll-region` (once — the pass is idempotent)
because the table itself is not the scrolling box; a `<pre>` already IS its
own scrolling box, so it is instrumented in place. Either way the scrolling
element carries the same conditional `tabindex`/`role`/`aria-label` as
`ScrollRegion`.

The name is the nearest heading preceding the surface — read for all of them
in one document-order pass — so a reader landing on the region hears which
section it belongs to; `labels` covers a surface with no heading above it, and
a name shared by several surfaces is numbered so no two regions answer to the
same one.

The pass runs from the ref callback rather than an effect, which puts it in
the commit phase, BEFORE the browser paints: `.tai-prose table` is
`width: 100%` with no overflow of its own — the scroller exists only on the
wrapper this builds — so a pass that ran after paint would show one frame at
320/360 px with the table overflowing the document, then jolt sideways. A
passive-effect probe measured exactly that: at first paint wrappers=0 and the
table's parent was `.tai-prose`.

Injected HTML is replaced wholesale when its source changes, so the pass is
re-run from a `MutationObserver` on the subtree rather than on attachment
alone.

**Props**

| Prop | Type | Description |
|---|---|---|
| `pre?` | `string` | — |
| `table?` | `string` | — |

**Related:** [ProseScrollLabels](/reference/studio-sdk/components#prosescrolllabels)


## useRegisterDirty

```ts
function useRegisterDirty(dirty: boolean): void
```

Report an editor's dirty state to the enclosing `DirtyGuardBoundary` (which
`GuardedTabs` also provides). A no-op outside a boundary (an editor rendered
standalone, e.g. in isolation tests) so the hook is always safe to call.

**Parameters**

| Parameter | Type | Default | Description |
|---|---|---|---|
| `dirty` | `boolean` | — | — |


## useViewMode

```ts
function useViewMode(
  surface: string,
  fallback: ViewMode,
): readonly [ViewMode, (mode: ViewMode) => void]
```

The persisted view mode for one `surface` (a stable key like `'tools'`). Returns
the stored choice or `fallback` when none is set, and a setter that writes through
to `localStorage`. Storage failures degrade to in-memory only — never a thrown
boot, matching the theme preference's defensive reads/writes.

**Parameters**

| Parameter | Type | Default | Description |
|---|---|---|---|
| `surface` | `string` | — | — |
| `fallback` | `ViewMode` | `'list'` | — |

**Related:** [ViewMode](/reference/studio-sdk/components#viewmode)

