{
  "name": "tai-docs-studio-sdk-typedoc",
  "version": "0.0.0",
  "private": true,
  "description": "Pinned TypeDoc toolchain owned by tai-docs for generating the Studio SDK reference. Kept out of the tai-studio monorepo so the Studio package manifests stay unchanged; gen_studio_sdk.py invokes this local typedoc binary against the tai-studio sources.",
  "license": "Apache-2.0",
  "devDependencies": {
    "typedoc": "^0.28",
    "typescript": "~5.9"
  }
}
